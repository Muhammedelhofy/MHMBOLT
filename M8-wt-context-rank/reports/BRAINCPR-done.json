{
  "build": "Build-110",
  "name": "Brain CPR — revive the 4 dead brain features",
  "branch": "merged to main; deployed to production",
  "production_sha": "b559468",
  "deployed": true,
  "status": "DONE — all 4 brain tables go 0 -> >0, live-verified in production. FREE fix (Groq), no Gemini credits.",
  "result_live": {
    "m8_reflections": "0 -> 5 (scored, overclaim-flagged, answers rewritten)",
    "m8_reasoning_chains": "0 -> 2 (decompose into 4 steps -> synthesized 'Therefore:' conclusion)",
    "m8_entities": "0 -> 3 (Atomic Habits[book], James Clear[person], Meditations[book])",
    "m8_entity_mentions": "0 -> 3"
  },
  "root_cause_was_a_stack_of_bugs": [
    "1. The 4 writes were un-awaited fire-and-forget — dropped by the Vercel freeze (waitUntil does NOT flush from M8's legacy (req,res) handlers, even with Fluid Compute). FIX: await the writes.",
    "2. The features never REACHED their writes: their scoring/decompose/extract ran on gemini-2.5-flash, a THINKING model whose hidden thinking ate the tiny output budget -> reply truncated BEFORE the JSON -> parseScore null / decompose n=1. FIX: non-thinking path.",
    "3. The obvious swap gemini-2.0-flash(-lite) is RETIRED (404), and the backup Gemini key is quota-capped (429). FIX: route the secondary calls to GROQ (free, fast, non-thinking Llama, SEPARATE quota from the main Gemini answer), cerebras fallback, gemini-2.5-flash-lite last.",
    "The brief's 'un-awaited writes' was real but only bug #1 of 3."
  ],
  "how_it_was_found": "Vercel's log tool collapses to ~1 line/request (blind to console.log), so a TEMPORARY m8_brain_debug table + instrumentation recorded the reflector/chain decision path to a queryable table. That revealed gate_eligible->enter->score_null and the raw truncated reply 'The answer fails' (16 chars). Diagnostics + table have been REMOVED.",
  "the_fix_files": [
    "lib/persistence.js (safePersist — waitUntil + never-throw, kept as belt-and-suspenders)",
    "lib/reflector.js (await logReflection; REFLECT_ORDER=groq,cerebras,gemini; REFLECT_MODEL=gemini-2.5-flash-lite; budget 8s)",
    "lib/reasoning-chain.js (await logChain; CHAIN_ORDER=groq,cerebras,gemini; CHAIN_MODEL=gemini-2.5-flash-lite; budget 18s)",
    "lib/entity-graph.js (extractor routed through llm.js on Groq; was a dead direct gemini-2.0-flash-lite call)",
    "lib/memory.js (await the entity extraction, bounded 6s race — Groq ~1s)",
    "lib/loop.js + lib/conjecture-memory.js (cron recordOutcome awaited — m8_conjecture_outcomes still needs the Lean lane / Build-B to produce leaves)",
    "vercel.json (fluid:true), package.json (@vercel/functions)"
  ],
  "cost": "$0 — all on the free providers (Groq/Cerebras). Gemini credits untouched. Turns are also FASTER (no thinking overhead, fast Groq).",
  "env_overrides_for_ops": ["M8_REFLECT_ORDER","M8_REFLECT_MODEL","M8_REFLECT_BUDGET_MS","M8_CHAIN_ORDER","M8_CHAIN_MODEL","M8_CHAIN_BUDGET_MS","M8_ENTITY_ORDER","M8_ENTITY_MODEL"],
  "tests": "tests/BRAINCPR-verify.ps1 44/44 (PS static mirror; host has no Node). Live-verified in production.",
  "reflector_scope": "UNCHANGED + verified: orchestrator gates the reflector to general/knowledge lanes only (never engine/compute/fleet). The narrow gate (also excludes compound + tutor) is WHY it runs only on single general questions — by design.",
  "item2_entity_cron_move": "DONE (f44d9e0): entity extraction MOVED from inline saveMemory (~1s/turn) to the nightly cron. New lib/memory.js sweepEntityExtraction() reads a watermark (m8_entity_cron_state, init now()), pulls user messages since, extracts+upserts (awaited in the cron handler so it lands), advances watermark. Wired into api/cron-summarize.js (own try/catch, ENTITY_SWEEP_DISABLED kill switch). Verified live: chat turns no longer extract inline (sapiens=0 immediately), conversation still saved, message correctly queued (pending_for_next_sweep=1). Cron is CRON_SECRET-gated so the SWEEP RUN itself confirms at the 02:00 UTC nightly (or Vercel dashboard manual Run).",
  "remaining": "m8_conjecture_outcomes still 0 despite 3 historically-verified Lean leaves — a REAL bug (recordOutcome likely never sees newlyVerified>0 because leaves verified in earlier runs and re-checks don't re-verify). Handed to a PARALLEL session (prompt: diagnose why outcomes don't write). Build-110 fixed that WRITE (awaited), not leaf production.",
  "not_touched": "Bolt fleet sync (separate repo), morning brief, the 4 cron schedules. Parallel Build-B (lean lane) integrated via rebase, not clobbered."
}
