# M8 Build Log

One row per build. Updated immediately when a build lands or is live-verified.
**Status keys:** ✅ LIVE-VERIFIED | 🟡 DONE (offline tests pass, live-verify pending) | 🔵 IN PROGRESS | ❌ BLOCKED

---

## Active builds

None. (Meaning-First v2: S1+S2 LIVE; C2 telemetry soak running; S3 next.)

---

## Shipped — Session 96 (2026-07-08, Opus · High — B-191: 4 live-transcript fixes + FIX 4b fix-forward)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| B-191 | ✅ **Four bugs from Muhammad's 2026-07-08 live transcript, root-causes pre-confirmed on host.** **FIX 1 (routing, `capability-registry.js`):** a trip-with-a-budget ("budget of 6000 sar … recommend a trip") scored `wallet:1`(WALLET_PRESENT `budget`)=`travel:1`(TRAVEL_PRESENT `trip`); the DOMAINS-order tie-break (wallet idx5<travel idx9) sent it to the wallet expense card AND swallowed his "from egypt" origin correction. `resolveIntent` now flips a **PRESENT-level (score 1) wallet⇄travel tie to TRAVEL** (a trip budget is planning, not an expense) — guarded to `top===1` so a STRONG wallet ("how much did I spend on my trip to Cairo" = wallet:2/travel:2) still wins. **FIX 2 (privacy — MOST URGENT, `knowledge-intake.js` + new `source-token-filter.js` + `orchestrator.js`):** vault docs are titled `vault:<path>.md` (`tools/vault-ingest.js`); `formatCitation` turned that into an injected `〔vault:…〕` citation the model echoed verbatim (`vault:Prime Claude.md` reached a user reply). Two layers: (a) `sanitizeSourceTitle` strips the `vault:` namespace before it becomes a citation; (b) `stripSourceTokens` — a code-guaranteed final-reply scrub (DO-sentinel pattern) removing any leaked `vault:…` / `【…】` token on BOTH the buffered and streaming paths, before the user OR persistence. **FIX 3 (honesty, `wallet.js` + the capabilityFallback card):** a "what's my balance" ask hit the generic add/edit/total card, implying a running balance exists; now answers honestly — *"I track spending, not a running balance — you've spent X SAR this month"* (`isBalanceQuery` gates it; a "spend" verb vetoes so "how much did I spend" still totals normally). **FIX 4 (`travel.js`):** "the trip is from Egypt" (a country) had no city/IATA, so the home default persisted; `travelClarify` now asks *"Which city in Egypt are you flying from?"* — never invents an airport. **FIX 4b (fix-forward, same session):** live self-verify showed FIX 4 was MASKED — the LLM trip-extractor silently resolves a country to its capital (Egypt→Cairo) upstream of the clarifier, so M8 assumed Cairo. `detectStatedCountryOrigin()` now deterministically catches "from `<country>`" in the user's own message and re-asserts it as `origin.city` (source=stated), **overriding the extractor's guess** — a code guarantee, so the ask fires. Anchored on a departure preposition (+ optional article, "from the UAE") so "outside Egypt" / "trip **to** Egypt" / a stated city ("from Cairo") never trigger it. No migration, no new env var. | ✅ LIVE-VERIFIED | `tests/build191_fixes.test.js` **43/43** + PS-5.1 ASCII mirror `tests/build191_fixes.test.ps1` **21/21**; FIX 1 in-fixture via `tests/intent_gate_test.js` **105/105** + PS mirror **154/0** (the present-tie flip mirrored); regression `build183_travel` **68/68** + `build189_travel_multi_destination` **31/31** unchanged; orchestrator + travel load clean. | **8462863** (ff-merge of `feat/b191-transcript-fixes` off `origin/main` @ `54cb7fd`, pushed to `main` on Muhammad's explicit **"merge and deploy"** then **"deploy FIX 4b"** OKs via `/deploy-verify`; fixes `bf37766`, FIX 4b `c992fd4`) | 2026-07-08 | Own worktree `M8-wt-b191`. Routing fixture pulled from `origin/fixture/b191-routing` @ `a0a176e` (byte-identical, 4 cases untouched — FIX 1 turns `travel-budget-recommend-trip` green). Touched `lib/capability-registry.js` (+11), `lib/knowledge-intake.js` (+18), `lib/orchestrator.js` (+35: FIX 2 strip both paths + FIX 3 card/enrich), `lib/travel.js` (+~85: FIX 4 + 4b), `lib/wallet.js` (+36), `tests/intent_gate_test.ps1` (+8). New `lib/source-token-filter.js`, `tests/build191_fixes.test.{js,ps1}`, `tests/BUILD191_LIVE_TEST.md`, `reports/build-191-done.json`. **PROD SELF-VERIFY (sha `aaed33a`→`8462863`, `m8-alpha.vercel.app`, dpls READY):** **FIX 1** *"…budget of 6000 sar…recommend a trip outside egypt…"* → a **travel** answer (Istanbul/Amman/Dubai within budget, "my take: Istanbul"), NOT the wallet card. **FIX 2** *"what do my notes say about how I like Claude to work?"* → cites **`START HERE`** / **`Status`** (relabeled note names) with **zero `vault:`/`【…】` token**, even on the *"is that classified info you cant pull?"* follow-up. **FIX 3** *"what is the balance of my wallet now?"* → **`"I track your spending, not a running balance — the wallet logs expenses, it doesn't hold a bank balance. You've spent 910.61 SAR this month…"`**. **FIX 4b** *"find me flights to Dubai next month, flying from Egypt"* → **`"Which city in Egypt are you flying from?"`** (before 4b it assumed Cairo + invited correction). |

---

## Shipped — Session 95 (2026-07-07/08, Opus · High + Fable · High C1 — Meaning-First Routing v2: S1 + S2 shadow)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| MV2-S1 | ✅ **The last-keyword-gap fixture — Muhammad's REAL phrasings.** Build step 0 of `M8_MEANING_FIRST_V2_SPEC.md` (completes B-176 against the #1 recurring pain). Mined **only his own app sessions** (`m8_conversations`, `session_[0-9]*`; the ~1300 autonomous/eval turns excluded) into 35 hand-labelled cases — 6 zero-keyword actions, 17 pending-action (accept/refuse/slot), 12 false-claim — **every phrasing verbatim, never invented**. Each zero-keyword label was verified against the REAL `scoreMessage()` (`writeMax===0`); every false-claim reply is the real DB reply (no lane sentinel). Honest finding baked in: the real damage isn't mis-routing (the E5 miss-loop is mined out) but the DOWNSTREAM dead-end — a zero-keyword write drops to the fallback LLM which then falsely DENIES the ability, falsely CLAIMS it acted, or ASKS a question that can't land. Fixture also caught a gap in the spec's own §4.4 done-claim regex (misses "Noted… marked as") before the detector existed. | ✅ LIVE (contract; no behaviour) | `tests/meaning_v2_test.js` PART 0 fixture-integrity green (registry confirms every zero-keyword label); PARTS 1-4 red-until-built. PS-5.1 mirror reference detectors green. | **1f4c26b** | 2026-07-07 | New `tests/fixtures/meaning_v2_phrasings.json`, `tests/meaning_v2_test.{js,ps1}`, `reports/meaning-v2-s1.md`. Branch `build/meaning-v2-s1` off `origin/main` @ `10d76b1`. |
| MV2-S2 | ✅ **CAPABILITIES single-source + claim-audit + DO-sentinel (SHADOW).** Spec steps 1-3. **Step 1 (fixes G6):** `capability-registry.js` gains `CAPABILITIES` (per-domain canDo/cantDo + label/blurb) + `buildAbilitiesPrompt()`; the abilities prompt is now COMPOSED from that single source (hand-typed body retired) and carries BOTH D4 rules — never-claim-**lack** AND the new never-claim-**done**. Byte-stable per deploy (B-178 cache intact); routing byte-identical (intent-gate 101/101 + 150/0). **Step 2:** `lib/claim-audit.js` — pure `detectDoneClaim` (§4.4 EXTENDED to catch the "Noted… marked as" false-done) + `detectCapabilityDenial`; wired telemetry-first at BOTH reply-finalize points — a done-claim/denial reaching the user WITHOUT a lane sentinel logs `claim-audit:false_done` / `:denial` (Stage-1 log only, zero behaviour). **Step 3:** `lib/do-sentinel.js` — pure writes-only `DO_MENU`, parse/strip marker, `M8_DO_SENTINEL` 3-state (default `shadow`), `shadowPromptRule()` with worked + counter example. Buffered fall-through turns get the shadow rule; a trailing `⟦DO:<domain>⟧` is stripped+logged **before the user or persistence see it**. Streaming-path shadow + `on`-mode re-entry deferred to C1/S4 (§4.1). | ✅ LIVE-VERIFIED (shadow; abilities-prompt live) | `meaning_v2_test.js` **66/67** (only S4 pending-action red, by design); PS mirror **69/73** (S4 only); regression **intent_gate 101/101 JS + 150/0 PS**; orchestrator loads clean; behavioural proof green (marker parse+strip, extended done-claim, denial, no false-positive). | **c7d5d93** (S2) → **ff68c90** (C1) | 2026-07-08 | Touched `lib/capability-registry.js`, `lib/orchestrator.js` (+91/−18: abilities swap, claim-audit + do-sentinel helpers + wiring). New `lib/claim-audit.js`, `lib/do-sentinel.js`. |
| MV2-C1 | ✅ **Fable C1 checkpoint — DO-sentinel review, approved + hardened.** [FABLE] review of the sentinel prompt wording + intercept diff (brief `SESSION_BRIEFS/MEANING_V2_C1_DO_SENTINEL_REVIEW.md`). Decisions: D1 tightened (counter-example + LAST-line placement), D2 fixed wider than asked (whitespace/case-tolerant markers + ASCII-bracket trailing+menu-bound fallback + wide DO-shaped strip; legit math `⟦n⟧` untouched), D3 defer streaming to S4, D4 keep `shadow` default, D5 keep denial logging. **Caught TWO real defects the brief missed:** (1) strip was gated on the trailing parse — a mid-reply/malformed marker reached the user verbatim → now unconditional + `do-sentinel:stray` telemetry; (2) the intercept ran AFTER `saveMemory` (marker persisted into memory) and after health-close/chips (un-trailed it, telemetry loss) → moved above the STORE, right after reflect; PS mirror pins the ordering. | ✅ LIVE-VERIFIED | included in the S2 counts above; PS mirror pins intercept ordering. | **ff68c90** (ff-merge of `build/meaning-v2-s1` off `origin/main` @ `10d76b1`, pushed to `main` on Muhammad's explicit **"yes - deploy to prod now"** via `/deploy-verify`) | 2026-07-08 | **PROD SELF-VERIFY (sha `ff68c90`, `m8-alpha.vercel.app`, prod dpl READY):** *"put down fifty riyals for lunch"* (his real phrasing that previously got the false *"I can't add expenses"*) → **`"Got it, Boss. I'll log 50 SAR for lunch in your wallet."`** — the false "can't" is GONE, no visible `⟦DO⟧` marker, claim-audit correctly quiet ("I'll log" is future, not "I've logged"). Shadow telemetry row confirmed in `m8_router_misses`: `arbiter:do-sentinel:wallet` / `mode=shadow band=none conf=0.50` — the recognizer works on real prod. ⚠️ Honest caveat: in shadow the write does NOT execute yet (the promise is unfulfilled until S4 turns recognition into a confirm→write). **Next:** C2 [FABLE] reads the shadow counts after a few days → `M8_DO_SENTINEL=on` flip decision; S3 (wallet/notes ladders) buildable anytime. |
| MV2-S3 | ✅ **Notes extraction ladder (`extractNoteLLM`), wallet SKIPPED after a spec↔code reconciliation.** Spec §4.3 named both wallet + notes ladders; reading the live code first showed **G2 is stale for wallet** — the wallet lane ALREADY has an LLM ladder (`classifyMoneyIntent`, the "Phase 1 intent brain": LLM reads kind+category, amount parsed deterministically, → confirm prompt whose "yes" survives via `parseConfirmExpensePrompt`). So `extractWalletLLM` would just DUPLICATE it, and the zero-keyword wallet case ("put down fifty riyals") fails at *routing* (band=none) which a `domain==="wallet"`-gated extractor wouldn't catch anyway (that's S4's DO-sentinel). Muhammad chose **notes-only** on the flagged fork. `extractNoteLLM` clones `extractTaskLLM` exactly: gated `M8_INTENT_GATE` + `M8_NOTE_EXTRACT` (default ON), spends the call ONLY when `resolveIntent(s).domain==="notes"`, temp 0 + shared provider waterfall, LLM normalise → canonical `"note: <content>"` → **deterministic re-parse via `parseNoteCapture`** (model gets no structural authority) → `{content}` to save else a specific ASK. Wired into `handleNotesCommand` after the regex miss and BEFORE the legacy free-form offers → **demotes the generic notes card to unreachable-for-notes** (the ladder's ASK fires first). Privacy: message TEXT only. | ✅ LIVE-VERIFIED | new `tests/meaning_v2_s3_notes.test.js` **14/14** (re-parse invariant `parseNoteCapture("note: "+X)===X`; the spend/kill gates short-circuit BEFORE any LLM call — non-notes→null, empty/long→null, `M8_NOTE_EXTRACT=0`→null; prompt shape) + PS-5.1 ASCII mirror `tests/meaning_v2_s3_notes.test.ps1` **14/14** (re-parse + source wiring + an **anti-drift guard** that `extractWalletLLM` must NOT exist and `classifyMoneyIntent` is the existing wallet ladder); regression `intent_gate_test.js` **101/101**; orchestrator loads clean. | **47fa2aa** (ff-merge of `build/meaning-v2-s3` off `origin/main` @ `f510775`, pushed to `main` on Muhammad's explicit **"yes - deploy to prod now"** via `/deploy-verify`) | 2026-07-08 | New `lib/orchestrator.js` (`extractNoteLLM` + `NOTE_EXTRACT_SYSTEM` + wiring; +~75), `tests/meaning_v2_s3_notes.test.{js,ps1}`, `tests/MEANING_V2_S3_LIVE_TEST.md`, `reports/meaning-v2-s3.md`. **PROD SELF-VERIFY (sha `47fa2aa`, `m8-alpha.vercel.app`, dpl READY):** *"jot in my notes that the … test ran"* (a note-routed phrasing `parseNoteCapture` MISSES) → **`"📝 Noted: \"the … test ran\"."`** — the LLM ladder normalised + saved, NOT the generic card. *"show my notes"* then listed it → the write is REAL (not a false "Noted"). Test note deleted from `m8_notes` after; his real "vehicle inspection" note untouched. |

---

## Shipped — Session 94 (2026-07-06, Sonnet · Med — CM3 classical-math checker pack v3)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| CM3 | ✅ **Classical-math checker pack v3 — Pythagorean triples**, the last diminishing CM leftover, extending CM1 (amicable/perfect/Euclid-Euler/Thabit) and CM2 (figurate identities/Pell) with 3 NEW generator-less, decidable templates: `pyth_triple` (verify a²+b²=c² for a given triple by direct computation), `pyth_primitive` (+ verify gcd(a,b,c)=1), `euclid_triple` (compute Euclid's formula a=m²−n², b=2mn, c=m²+n² at a given m,n and verify it's primitive — checking m>n>0, coprime, opposite parity, then the resulting gcd). Every value stays well under 2^53 (`TRIPLE_MAX`=1e7, `EUCLID_MN_MAX`=3000), so **no BigInt needed** — the first CM family to be exact in plain `Number`. Own kill-switch `M8_CLASSICAL_MATH_V3` (default ON; OFF ⇒ the 3 templates rejected exactly as if absent, `detectKernelTest` stops routing Pythagorean-triple questions, and NONE of the digital-root / CM1 / CM2 proposer prompts are touched — proven byte-identical). Own seed pack `data/seed-packs/classical-math-v3.json` (R2 schema, 5 seeds): 3 bound to the new checkers — the triple definition (Euclid, *Elements*, Book I, Prop. 47, the "windmill" proof), the primitive definition (Wolfram MathWorld), and Euclid's formula itself (*Elements*, Book X, Lemma 1 preceding Prop. 29) — plus 2 honestly unbound (`matches_templates: []`): a mysticism LEAP (Plutarch, *De Iside et Osiride* §56 — the 3-4-5 triangle read as Osiris/Isis/Horus) and a context seed (Plimpton 322, the Old Babylonian tablet that tabulated Pythagorean triples ~1000 years before Pythagoras). Own proposer + own detection regex (`pythagorean triples?`/`euclid'?s? formula`/`primitive triples?`) — disjoint from CM1/CM2 vocab, and (mirroring the CM2 precedent) a bare mystical "is the 3-4-5 triangle sacred?" question does NOT fire this detector — the general kernel/leap honesty spine handles pure mysticism, not this checker. Routed through the SAME hard-route as CM1/CM2 (zero `orchestrator.js` edits, zero new `api/` fn, zero migration). Every headline figure (3-4-5, 5-12-13, 8-15-17, 20-21-29, 7-24-25, 9-40-41, 12-35-37 primitive; 6-8-10, 9-12-15 non-primitive; Euclid's formula for 8 (m,n) pairs incl. the non-coprime m=4,n=2 case) was independently verified before shipping, and every citation was web-verified against a real source (Clark University's Euclid transcription, Wolfram MathWorld, Perseus Digital Library, Wikipedia, ISAW/NYU) — FP=0. | ✅ LIVE-VERIFIED | new `tests/buildCM3_classical_math_v3.test.js` **122/122** (pure core: gcd + Euclid's-formula construction; all 3 checkers verified against the canon incl. non-primitive/precondition-failure cases; validateClaim boundary caps incl. the m≤n degenerate-rejection; seed schema + code-guaranteed citation on hold, none on a false result; kill-switch OFF-identity — byte-identical digital-root **and** CM1 **and** CM2 prompts; detection precision incl. the mysticism-question non-fire); PS-5.1 ASCII mirror `tests/buildCM3_classical_math_v3.test.ps1` **64/64** (every value exact in `[long]`, no `[bigint]` needed — the first CM family this simple); full regression green: `buildCM1` **99/99**, `buildCM2` **107/107**, `buildR3_baselens` **67/67**, `buildR3_lean_ladder` **35/35**, `buildE5_dr_proposer_examples` **33/33**, `buildR1_cited_recall` **74/74 (ALL GREEN)**, `buildR2_seedpack_wiring` **51/51**, `buildR4_health_rail` **71/71**. | **4a89856** (ff-merge of `build/cm3-classical-math-v3` off `origin/main` at `8206577`, pushed to `main` on Muhammad's explicit **"Merge + deploy now"** OK) | 2026-07-06 | Own worktree `M8-wt-cm3`. Touched only `lib/kernel-conjecture.js` (+286 lines, additive-only — no deletions). New files `data/seed-packs/classical-math-v3.json`, `tests/buildCM3_classical_math_v3.test.{js,ps1}`, `tests/BUILD_CM3_LIVE_TEST.md`. Optional stretch (continued-fraction convergents of e/π) was explicitly SKIPPED per the build brief's "keep CM3 bounded" instruction. **PROD SELF-VERIFY (sha `4a89856`, `m8-alpha.vercel.app`):** *"is (3,4,5) a Pythagorean triple?"* → ✅ VERIFIED + 📚 cited Euclid Book I Prop. 47. *"generate a primitive triple from m=2, n=1"* → ✅ VERIFIED (3,4,5) + 📚 cited Euclid Book X Lemma 1. *"does Euclid's formula give a primitive triple for m=4, n=2?"* → ❌ honest — computed (12,16,20) but NOT primitive (preconditions unmet), no citation on the false result. *"did you prove the 3-4-5 triangle is sacred?"* → honest "No" (no fabricated proof; confirmed the CM3 detector correctly stood down — no ✅/❌/📚 classical-math formatting hijacked the turn). Regression canaries on prod: `are 220 and 284 amicable?` still CM1 ✅ + citation; `is every hexagonal number triangular?` still CM2 ✅ + citation — no lane theft. |

---

## Shipped — Session 93 (2026-07-06, Opus · Med — R3 Lean ladder wiring)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| R3-LADDER | ✅ **The production caller the R3 stretch prover never had** (Path 1 of `reports/M8_LEAN_FEASIBILITY_SPIKE.md` §D). `leanVerifyDigitSumMod9()` shipped in R3 but was "wired to nothing"; B-190 then proved the theorem lives end-to-end via the *chat* lane (node 659). This build connects the two: when `runKernelTest` shows a **HELD base-10 digital-root pattern** (the ✅ OBSERVED-to-N block), it appends a **PROVEN tier** carrying Lean's real 0-sorry receipt for the mod-9 digit-sum congruence `(10a+b)%9=(a+b)%9` — the FOUNDATION that makes every base-10 digital root well-defined. Strictly separated from the two weaker tiers already rendered: the leap = **SPECULATIVE** (untouched `leapNote`), this pattern = **OBSERVED-to-N**, the mod-9 congruence beneath it = **PROVEN**. The proven tier scopes to the GROUNDWORK only — it explicitly says it does NOT prove the specific period/constant/set. New `leanLadderLine(claim, result, {verifyFn})` (DI-injectable, mirrors the R3 fn's DI) appended at all **4 held-return points** in `runKernelTest` (literal / E5-raw `renderBestKernel` / single / decomposed). **Honesty-tight scope:** only `dr_periodic`/`dr_constant`/`dr_set` (base 10); `mod_cycle` (arbitrary modulus, not mod 9) and `dr_base_*` (base b≠10 rests on mod b−1) are EXCLUDED — the theorem is `10a+b`, mod-9 only. **Fail-safe:** checker cold/unreachable/not-verified ⇒ an honest `⏳ Machine-check pending` note, NEVER a fabricated proof (proven line code-guarded by `status==='proven'`). Kill-switch `M8_LEAN_LADDER` (default ON); OFF ⇒ `leanLadderLine` returns `""` ⇒ byte-identical. **Narration-only** — zero cron, zero DB write, zero routing change, no new `api/` fn, no SQL migration. | ✅ LIVE-VERIFIED | new `tests/buildR3_lean_ladder.test.js` **35/35** (kill-switch default-ON + off/OFF byte-identical; eligibility scoping incl. mod_cycle + dr_base_* + falsified + null EXCLUDED; proven tier carries real receipt + scopes to groundwork + names 3 tiers; cold/error/not-verified/thrown all fail safe to pending-or-empty, never the proven claim; static wire-guards proving all 4 held-return points call `leanLadderLine` + the proven line is code-guarded by `status==='proven'`); PS-5.1 ASCII mirror `tests/buildR3_lean_ladder.test.ps1` **21/21**; regression: `buildR3_baselens` **67/67**, `buildR2_seedpack_wiring` **51/51**, `buildCM1` **99/99**, `buildCM2` **107/107** all still green. | **c3c9c5d** (ff-merge of `build/lean-ladder-r3` off `origin/main` at `a03b21e`, pushed to `main` on Muhammad's explicit **"Deploy now, verify on prod"** OK) | 2026-07-06 | Own worktree `M8-wt-lean-ladder`. Touched `lib/kernel-conjecture.js` (+`leanLadderEnabled`/`LEAN_LADDER_TEMPLATES`/`leanLadderEligible`/`leanLadderLine` + exports + 4 wire points). New files `tests/buildR3_lean_ladder.test.{js,ps1}`. **PROD SELF-VERIFY (sha `c3c9c5d`, `m8-alpha.vercel.app`, two-stage):** (1) COLD-checker deploy confirmation — *"test the doubling digital root"* returned the observed block for `dr(9n)=9` **plus** the new `⏳ Machine-check pending` line (old code would have no ladder line), proving the wiring is live AND the fail-safe honest. (2) WARM checker (Cloud Run `/health` polled to `ready:true` after ~10.5 min cold Mathlib import) — *"test the claim that the digital root of 2^n repeats with period 6"* returned the FULL ladder: ✅ OBSERVED-to-N (period 6, n=10,000) + 📚 cited known-math (OEIS A000079, ord₉(2)=6) + **⚡ PROVEN (Lean-verified): `theorem base10_two_digit_mod9 (a b : ℕ) : (10 * a + b) % 9 = (a + b) % 9 := by omega`, 0 sorry / 0 errors** — a real machine-checked receipt from the live service, with the proven tier correctly scoping to the GROUNDWORK, not the specific period-6 pattern. Note the mod_cycle/base-b exclusion also confirmed live: a run where the proposer picked `2^n mod 7` (period 3) correctly rendered NO proven tier. |

---

## Shipped — Session 92 (2026-07-06, Sonnet · Med — Fleet-staleness guard — Build-S1)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| S-1 | ✅ **Post-Bolt-job-exit safety net.** M8's fleet lane reads the Bolt dashboard's `fleet_data` Supabase row, kept fresh only by the dashboard's own nightly sync crons. Once Muhammad leaves the Bolt job (~Jul 2026) those crons stop for good and the row freezes at its last value — without a guard M8 would keep answering fleet questions ("drivers today: X") as if the frozen numbers were live. `detectFleetStale(record, entries)` (new, `lib/fleet.js`) is a pure detector: STALE once the row's last sync (`record._syncedAt`, stamped from Supabase's `updated_at` by the existing `fetchFleetRecord`) is older than `M8_FLEET_STALE_DAYS` (default 3) — a much longer horizon than the existing `fleetFreshness()`'s 18h "sync pending" signal (that one only flags a single missed nightly sync for the morning brief). `asOfDate` is the newest FLEET-DAY on record (what the frozen numbers actually date to), not the sync timestamp. Fails safe: an unknown sync time never marks data stale. `fleetStaleDirective(staleInfo)` frames the answer as an honestly-dated ARCHIVE — leads with `"⚠️ Fleet data is frozen as of ⟨date⟩ (the nightly sync has stopped) — these are historical numbers, not current"`, forbids "today/currently/now/live" wording, and explicitly says this is NOT a refusal (still answers with the real figures — this is his CV/job-hunt archive). Wired into `lib/orchestrator.js` at both compose sites (buffered + streaming), same shape as the Build-R4 health rail: runs LAST among the `fleetCtx.text` mutations (after change-analysis/report overwrites) so it can never be silently dropped by a later overwrite. Kill-switch `M8_FLEET_STALE_GUARD` (default ON); OFF or fresh data is a byte-identical no-op. **Zero cron, zero DB write, zero routing change** — a read-only compose-time narration guard only. | ✅ LIVE-VERIFIED | new `tests/buildS1_fleet_stale_guard.test.js` **31/31** (kill-switch default-ON + off/0/OFF case-insensitivity; detectFleetStale fresh/stale/borderline/unknown-sync-time-fails-safe/no-entries cases; fleetStaleDirective framing text incl. forbidden today/current/now/live wording + explicit not-a-refusal + missing-asOfDate fallback; static wire-guards proving both orchestrator compose sites gate on `fleetCtx.text && fleetStaleGuardEnabled()`, call `fleetStaleDirective(...)` exactly twice, and the guard runs before `fleetCtx.text` folds into `systemInstruction`); PS-5.1 ASCII mirror `tests/buildS1_fleet_stale_guard.test.ps1` **27/27**; regression: existing `tests/fleet-test.js` **226/230** — same 4 pre-existing date-dependent failures confirmed via `git stash` against unmodified `main` (unrelated to this build); both edited files `require()`-load without error. | **02a6421** (merge commit `cedbb42`→`02a6421`, pushed to `main` on Muhammad's explicit **"yes please merge and deploy"** OK) | 2026-07-06 | Own branch `build/s1-fleet-stale-guard` off `origin/main` at `781f6d0`. Touched `lib/fleet.js` (+44 lines: `detectFleetStale`/`fleetStaleGuardEnabled`/`fleetStaleDirective` + exports), `lib/orchestrator.js` (+33/-1: import + guard block at both the buffered and streaming compose sites). New files: `tests/buildS1_fleet_stale_guard.test.{js,ps1}`. **PROD SELF-VERIFY (sha `02a6421`, `m8-alpha.vercel.app`, READY within ~15s of push):** since prod fleet data is currently FRESH, the stale path can't be exercised live (that's proven by the offline test above) — self-verify instead confirms the guard stays SILENT on fresh data: *"how many drivers do we have today"* → `"Boss, we have 49 drivers in the fleet today."`, a clean normal fleet answer with no staleness framing, exactly as before this build. |

---

## Shipped — Session 91 (2026-07-06, Sonnet · Med — Lean formalize truncation fix)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| B-190 | ✅ **Fixes the live Path-0 failure** (the Lean spike's "zero code" demo actually truncated mid-statement twice, `lean_rejected`). Root cause: `gemini-2.5-flash` defaults to dynamic/auto thinking whenever `thinkingConfig` is omitted from the request (true "off" only for 2.0/1.5-flash) — reproduced live, ~650-670 of the Lean draft's 700-token cap silently burned on invisible thoughts, leaving a razor-thin margin that truncated unpredictably (`finishReason: MAX_TOKENS` reproduced directly). A second bug masked any fix attempt: `buildGeminiConfig`'s guard `if (thinkingBudget > 0)` silently dropped an explicit `0`, so `lib/lean.js` (and 3 other callers already passing `thinkingBudget:0` — `domain-arbiter.js`, `intent-router.js`, `orchestrator.js:1606`) could never actually disable it. Fix: `llm.js` now sends `thinkingConfig` whenever a budget is explicitly requested (incl. 0); only omits the field when nothing was specified (unchanged for every other caller). `lib/lean.js`'s formalize call sets `thinkingBudget:0` explicit + `maxOutputTokens` raised to 1024. | ✅ LIVE-VERIFIED | new `tests/lean_thinking_budget.test.js` **7/7** (explicit 0 sends config, positive budget unaffected, unset omits field, env-set path, per-call overrides env); existing Lean suite `tests/lean-verify.js` **38/38** unaffected; full `tests/*.test.js` sweep green. | **dbd5e7d** (pushed straight to `main`, Muhammad's explicit **"merge, then check"** OK) | 2026-07-06 | Own worktree (`M8-wt-lean-fix`, branch `claude/lean-formalize-fix`, off `origin/main` at `ba2428e`). Touched `lib/llm.js` (`buildGeminiConfig` thinking-budget gate + exported for testing), `lib/lean.js` (formalize genConfig). New file `tests/lean_thinking_budget.test.js` (+ live-repro helper `tests/lean_formalize_truncation_probe.js`, not part of the offline suite — needs a real Gemini key). **PROD SELF-VERIFY (sha `dbd5e7d`, `m8-alpha.vercel.app`):** the exact live-failure ask — *"formalize and verify in Lean: for all natural numbers a and b, (10*a + b) % 9 = (a + b) % 9"* — came back **"✓ Lean Verified"** (0 errors, 0 `sorry`, `theorem mod_nine_property (a b : ℕ) : (10 * a + b) % 9 = (a + b) % 9 := by omega`), and a real `proven` node landed in `m8_graph_nodes` (`id 659`, `2026-07-06 11:28:51 UTC` — the first machine-proven theorem in Muhammad's own digital-root/mod-9 domain, confirmed by read-only SQL against the BOLT Supabase project). Corrects the vault Path-0 note: this was never "zero code" — it needed this fix. |

---

## Shipped — Session 89 (2026-07-06, Sonnet · Med — Travel lane multi-candidate destination fix — B-189)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| B-189 | ✅ **Fixes the known gap flagged (not fixed) in B-187:** `extractTripState()`'s trip schema modeled a single `destination.city`, so a message naming two candidate destinations ("hurghada or sharm") gave the extractor nothing valid to put there — `normalizeTrip` returned null and the WHOLE travel packet (links, live flight/hotel search) silently skipped that turn, degrading straight to the non-travel path. **Fix (meaning-first, no new keyword lane — still the ONE LLM extraction call):** the JSON schema gains an optional `"destinationCandidates":[{city,iata}]` field, filled ONLY when the model reads 2+ named candidates with no decided destination. `normalizeTrip` (`lib/travel.js`) keeps the trip alive in that case (`destination:null`, candidates deduped case-insensitively + capped at 4) instead of discarding it. The EXISTING clarify-once mechanism (`travelClarify`, same one-blocking-question pattern the missing-dates case already uses) now checks the ambiguous-destination case FIRST — before origin/dates are ever touched — and returns the ONE disambiguating question via a new pure composer `destinationChoiceClarify` ("Hurghada or Sharm El Sheikh — which one did you mean?", Arabic mirrored). `orchestrator.js`'s gate widens to reach `travelClarify` on the ambiguous branch too (previously only reachable with a resolved single destination); that branch always returns a clarify string by construction, so the packet-building code (links/flights/hotels) is unreachable until he picks one — `resolveOrigin`/`canonicalizeTripIata` are now explicitly gated to `_hasDest` only (previously unconditional; would have thrown on a null destination). Rebased onto the concurrent B-188 direct-booking-links build (Session 87b, `dad5ae1`) — non-overlapping insertions in `lib/travel.js`/`lib/orchestrator.js`, no logic conflict. See `M8_TRAVEL_LANE_SPEC.md` Amendment B4. **Zero migration · zero new key · zero new `api/` fn.** | ✅ LIVE-VERIFIED | new `tests/build189_travel_multi_destination.test.js` **31/31** (normalizeTrip destinationCandidates parsing — 2-candidate keeps trip alive, 1-candidate still null, garbage entries skipped, case-insensitive dedupe, capped at 4, a decided destination wins and candidates are ignored, non-array/absent unchanged; `destinationChoiceClarify` EN 2-and-3-candidate list composition + exactly-one-question-mark, Arabic mirror; `travelClarify` precedence — ambiguous destination fires before any origin/dates logic, single-candidate not mistaken for ambiguous, regression: resolved-destination clarify and fully-resolved-trip null both unchanged; static wire-guard greps on `orchestrator.js` proving the widened gate + guarded `resolveOrigin` call + `travelClarify` still short-circuits); PS-5.1 ASCII mirror `tests/build189_travel_multi_destination.test.ps1` **31/31**; added routing canary `travel-two-candidate-dest` to `tests/fixtures/routing_phrasings.json` (the real "check hotels in Hurghada or Sharm..." transcript shape that triggered the B-187 fleet-leak incident — proves the ROUTING layer still resolves `travel`, never `fleet`, independent of the downstream extractor fix); `intent_gate_test.js` **101/101** (was 100/100); regression (post-rebase, re-run against `dad5ae1`): `build183_travel.test.js` **68/68**, `build187_hotel_search.test.js` **56/56**, `build188_direct_booking_links.test.js` **43/43** unaffected; full `tests/*.test.js` sweep green, full `tests/*.test.ps1` sweep green; `orchestrator.js` `require()`-loads without error. | **18b9af6** (fast-forward pushed straight to `main` from the rebased branch — no local `main` checkout touched, since the shared `Bolt/M8` working copy had unrelated pre-existing local edits (`NORTH_STAR.md`, `AGENTS.md`, `.claude/`) that were not mine to disturb; pushed on Muhammad's explicit **"deploy please"** OK; `/api/health` sha `18b9af6` — Supabase ok + 6 free providers, READY within ~60s of push) | 2026-07-06 | Own worktree (`M8-travel-multidest`, branch `claude/travel-multi-destination`, originally off `origin/main` at `b4d29a1`, rebased onto `dad5ae1` (Session 87b's B-188) after that build landed mid-session — a real parallel-commit collision on the SAME build-number label; renumbered this build B-188→B-189 and this session 88→89 to resolve it. Touched `lib/travel.js` (extractor schema + `normalizeTrip` + `travelClarify` + new `destinationChoiceClarify`, both exported — coexists non-overlapping with B-188's `renderDirectBookingLinkBlock`), `lib/orchestrator.js` (gate widened + `resolveOrigin`/`canonicalizeTripIata` guarded to `_hasDest`), `M8_TRAVEL_LANE_SPEC.md` (Amendment B4), `tests/fixtures/routing_phrasings.json` (+1 canary). New files: `tests/build189_travel_multi_destination.test.{js,ps1}`. **PROD SELF-VERIFY (sha `18b9af6`, `eval-b189v-*` sessions — fully ephemeral, `isEphemeralSession` in `lib/memory.js` skips both recall AND save for any `eval`-prefixed sessionId, confirmed by source read, nothing to purge):** ① **★ FLAGSHIP** — the exact incident phrasing *"check for me best hotels in Hurghada or Sharm from the 15th to the 20th of August"* → **`"Hurghada or Sharm — which one did you mean?"`** — the ONE clean disambiguating question, not a fabricated price table, not a fleet-error leak, not a silent non-travel degrade. ② **FOLLOW-UP RESOLUTION** — replying *"Sharm"* (with `history` carrying both prior turns, per M8's client-supplied-history contract) correctly resolved the destination and proceeded to a real hotel answer (web-search-sourced table with honest per-source attribution — `SERPAPI_KEY`/`M8_TRAVEL_HOTELS` dark in this prod env so it fell through to the pre-existing web-search waterfall tier rather than the SerpApi structured tier, which is the correct fail-safe behavior, not a regression) — proving the ambiguous branch doesn't dead-end the conversation. **③ REGRESSION** — *"how are my drivers doing today"* → full fleet morning brief (50,239 SAR gross, +34%, driver-by-driver breakdown) with **no travel-lane hijack**, confirming the widened orchestrator gate didn't touch fleet routing. |

---

## Shipped — Session 87 (2026-07-06, Sonnet · Med — Travel lane hotels extension + fleet-no-data scope fix — B-187)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| B-187 | ✅ **Two bugs found from a real M8 conversation transcript Muhammad shared, both fixed.** **Bug 1 (fleet-error leak):** a Hurghada hotel-search turn got answered with "I don't have your fleet data loaded for that question, Boss" — a fleet-domain canned reply with nothing to do with the question. Root cause: `orchestrator.js`'s `RULE_FLEET_NO_DATA` (line 95) fires on EVERY turn without a loaded fleet packet, regardless of the turn's actual topic — it never scoped its trigger to fleet-shaped questions. Fixed by rewording the rule to explicitly state it applies ONLY when Muhammad's current question is fleet/driver/business-shaped; the anti-fabrication guarantee for real fleet questions is unchanged. This is a mechanism-agnostic fix — it holds regardless of why a travel/other packet failed to build that turn (a background investigation traced the ACTUAL trigger to `extractTripState()` being unable to parse a two-candidate destination, "Hurghada or Sharm", so the whole travel packet silently skipped that turn — flagged as a known gap below, not fixed here). **Bug 2 (fabricated hotel data):** the same conversation's hotel search returned a price table explicitly labeled "based on 2024 Booking.com data" — fabricated, not live. Root cause: hotels never got flights' B-184 SerpApi upgrade (§10 ④ of the spec explicitly deferred it) — only a Booking.com link builder + generic web-search query existed, no structured hotel API. Fixed by adding `lib/tools/hotelSearch.js` (SerpApi `engine=google_hotels`, mirrors `flightSearch.js` exactly: same key, same 7s timeout, same fail-safe waterfall, same D8 read-only/no-booking boundary) and wiring it into `lib/travel.js` (`hotelsEnabled`/`planHotelSearch`/`renderHotelsBlock`) + `orchestrator.js` (tier-0 hotel call alongside the existing flight call, `travelSearchPlan` now also supports `skipHotels`). See spec Amendment B2. **Zero new key** (reuses `SERPAPI_KEY`) **· zero migration · zero new `api/` fn.** | ✅ LIVE-VERIFIED (offline) | new `tests/build187_hotel_search.test.js` **56/56** (normalizeHotels pure mapping incl. cheapest-first sort + unpriced-sinks-to-end; hotelsEnabled gate; planHotelSearch params + all null/fall-through cases incl. missing check-out; renderHotelsBlock incl. empty/no-name-skip; buildTravelPacket HOTELS-above-LINKS + hotelCount + Phase-A byte-identity with no offers; travelSearchPlan skipHotels; D8 payment-boundary source greps; mocked-fetch privacy-wall request-shape assertions — allowlisted query keys only, key-gate throws, missing-check-out-date throws); PS-5.1 ASCII mirror `tests/build187_hotel_search.test.ps1` **44/44**; regression: `build184_flight_search.test.js` **62/62** unaffected, `build169d_prompt_diet.test.ps1` **48/48** (the RULE_FLEET_NO_DATA prose edit doesn't touch the structural section-selection logic this test asserts); full `tests/*.test.ps1` sweep **41/41 files green**, `orchestrator.js` `require()`-loads without error. | — (uncommitted this session — see conversation) | 2026-07-06 | Touched `lib/orchestrator.js` (RULE_FLEET_NO_DATA prose + the new hotel tier-0 call block), `lib/travel.js` (D6b hotels helpers + directive wording + buildTravelPacket/travelSearchPlan hotel support), `M8_TRAVEL_LANE_SPEC.md` (Amendment B2). New files: `lib/tools/hotelSearch.js`, `tests/build187_hotel_search.test.{js,ps1}`. **Known gap flagged, not fixed:** `extractTripState()` models a single `destination.city` — a multi-candidate destination phrasing ("Hurghada or Sharm") can silently fail to reconstruct a trip, skipping the whole travel packet for that turn; worth a future session if it recurs in `m8_router_misses`. **Not yet deployed** — no commit/push/deploy happened this session; awaiting Muhammad's explicit OK per the deploy-then-self-verify convention. |

---

## Shipped — Session 87b (2026-07-06, Sonnet · Med — Direct booking links: per-hotel + fetch-on-pick flights — B-188)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| B-188 | ✅ **Muhammad asked why M8 only gives a generic search-page link, never a direct link to the specific recommended flight/hotel.** Two fixes, per his explicit choice on the flights trade-off (see spec Amendment B3). **(1) Hotels (no trade-off):** `hotelSearch.js`'s `normalizeHotels()` now captures a `link` per offer — a named OTA-site link from SerpApi's `prices[]` array (e.g. a direct Booking.com URL for that exact property) preferred over the generic Google-Hotels `property.link`; `renderHotelsBlock()` shows it per row. Zero new cost — SerpApi already returned this. **(2) Flights (fetch-on-pick, his recommended trade-off):** SerpApi's flight search has NO bookable link, only an opaque `booking_token` — getting a real link needs a SECOND SerpApi call. Rather than burn quota fetching it for all 5 offers shown, `normalizeFlights()` now captures `bookingToken`, and two new pure helpers in `travel.js` — `wantsFlightBookingLink()` (signal: "book/select/link for the first/cheapest/#N one") + `resolveFlightSelection()` (matches the ask against THIS turn's fresh offer list by ordinal/cheapest/unique-carrier — null on any ambiguity, never guesses) — gate a new `lib/tools/flightSearch.js:getBookingLink()` call that redeems ONLY the picked offer's token, via `orchestrator.js`. `extractBookingLink()` only surfaces a plain clickable GET link, deliberately rejecting a POST-only `booking_request` as unusable rather than handing over a broken link. A DIRECT BOOKING LINK block renders above the generic links when redemption succeeds; any failure/no-match/no-plain-link silently degrades to the generic links, unchanged from before. **Zero new key (reuses `SERPAPI_KEY`) · zero migration · zero new `api/` fn.** | 🟡 DONE (offline tests pass; flight-link redemption schema live-verified via a real prod conversation, not a byte-exact API contract test) | new `tests/build188_direct_booking_links.test.js` **43/43** (hotel link capture incl. OTA-link-preferred-over-property-link + null-when-absent; renderHotelsBlock includes the link; flight bookingToken capture; extractBookingLink pure parsing incl. rejecting a POST-only booking_request, checking together/departing/arriving leg shapes, skipping a non-qualifying option; wantsFlightBookingLink signal detection incl. false-positive guard; resolveFlightSelection ordinal/cheapest/carrier-match/ambiguous-null/out-of-range-null/empty-offers-null; renderDirectBookingLinkBlock + buildTravelPacket wiring above BOOKING LINKS + Phase-A byte-identity when absent; directive LINKS-rule update; D8 source greps; mocked-fetch getBookingLink request-shape/privacy-wall + key-gate + param-gate); PS-5.1 ASCII mirror `tests/build188_direct_booking_links.test.ps1` **34/34**; regression: `build184_flight_search.test.js` **62/62**, `build187_hotel_search.test.js` **56/56**, `build183_travel.test.js` **68/68** all unaffected; full `tests/*.test.ps1` sweep **42/42 files green**; all 4 touched modules `require()`-load without error. **PROD SELF-VERIFY (sha `dad5ae1`, `m8-alpha.vercel.app`):** ① a real Hurghada hotel search (15-20 Aug 2026) returned a **genuine per-hotel Booking.com link on each row** (`https://www.booking.com/hotel/eg/<slug>.en-gb.html` per property), not just one generic destination-wide search link — the hotel half of this build confirmed working exactly as designed, zero extra cost. ② a real RUH→HBE flight search (first turn) followed by "book the cheapest one" (second turn, WITH the conversation `history` array — a first attempt without `history` mis-routed to the unrelated free-form-task-offer flow, which is the correct behavior of the PRE-EXISTING B-183 guard given M8's client-supplied-history contract, not a bug in this build) correctly identified "Air Arabia Egypt E5 336 – SAR 685" as the selected flight in its answer, attempted redemption, found no plain-GET link from SerpApi for that route, and **degraded honestly** to the generic Google Flights link with a plain "I'm unable to finalize the purchase for you" — never a broken or fabricated URL. This is the expected fail-safe behavior; it does NOT yet prove a plain-GET `DIRECT BOOKING LINK` can be produced for a real Google Flights route (see honest caveat below — Google Flights' `booking_options` are commonly POST-redirect-based, so this path may rarely fire in practice even when working correctly; re-verify on more routes/carriers before assuming it's dead code). | `dad5ae1` (pushed to main on Muhammad's explicit **"Yes, commit and push/deploy now"** OK) | 2026-07-06 | Same session as B-187 (Session 87), continued after Muhammad's follow-up question. Touched `lib/orchestrator.js` (fetch-on-pick redemption block), `lib/travel.js` (D6c selection/render helpers + directive LINKS-rule update + buildTravelPacket directBookingLink wiring), `lib/tools/flightSearch.js` (bookingToken capture + getBookingLink/extractBookingLink), `lib/tools/hotelSearch.js` (`_pickHotelLink`), `M8_TRAVEL_LANE_SPEC.md` (Amendment B3). New files: `tests/build188_direct_booking_links.test.{js,ps1}`. **Honest caveat:** `extractBookingLink()`'s parsing of SerpApi's `booking_options` shape (the `together`/`departing`/`arriving` leg keys, the `booking_request.url`/`post_data` fields) is written defensively from documented/expected SerpApi schema and unit-tested against a MOCKED response — it has NOT been byte-verified against SerpApi's actual current response shape with a real key from this session (the prod self-verify confirms the END-TO-END FLOW behaves safely — real link OR honest fallback, never broken — but doesn't prove the exact field-name parsing is hitting on every real SerpApi response). If a future `m8_router_misses`/telemetry check shows `travel_booking_link_empty` firing on routes that plausibly DO have a bookable offer, re-verify the parser against a fresh real payload. |

---

## Shipped — Session 86 (2026-07-05, Opus · Med — Infra hygiene: Vercel Hobby function consolidation pass 2)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| — | ✅ **Vercel Hobby 12-function consolidation, pass 2 (merges A+B) — 12 → 10 functions, 2 slots freed.** Audit-first infra pass to reclaim headroom under the Hobby 12-fn cap so a future feature has a slot (pass 1, 2026-06-21, took 30→10; the two features added since — `tasks`, `transcribe` — had eaten both headroom slots back to 12/12). **Audit** mapped all 12 `api/*.js`: 4 existing `?fn=` routers (knowledge/files/export/ops), 4 crons (cron-summarize/explore/verify + morning-brief) + push-cron-via-ops = the hands-off zone, and 4 standalones. Muhammad approved **only** the two safe (visible-if-broken, non-cron) merges. **A:** `api/chat-stream.js` folded into `api/chat.js` as a `?fn=` router (`?fn=stream` → SSE; no-fn/`buffered` → the original buffered flow). **B:** `api/tasks.js` + `api/transcribe.js` → new `api/util.js` router (`?fn=tasks\|transcribe`). Handler bodies relocated **VERBATIM** to `lib/handlers/` (git detected renames; only `../lib/X`→`../X` require paths + header comments changed — zero logic drift, confirmed by diff). `util.js` owns config: `maxDuration:30` (≥ tasks' old 15 + transcribe's default) + `bodyParser:12mb` (transcribe's need; harmless to tasks). `vercel.json`: 3 new rewrites (`/api/chat-stream`→`chat?fn=stream`, `/api/tasks`→`util?fn=tasks`, `/api/transcribe`→`util?fn=transcribe`, catch-all still last) + `chat-stream.js` dropped from the functions block. **All 5 crons UNCHANGED** (never entered the hands-off zone). **No dead endpoints found** (pass 1 already deleted them). **Zero migration · free-stack · no new key.** | ✅ LIVE-VERIFIED | `tests/consolidate-fns-ab-verify.js` **42/0** (mocks handlers via `require.cache` to actually exercise router dispatch: `fn=stream`→stream, no-fn→buffered, `fn=buffered`→buffered, unknown→404; util `fn=tasks/transcribe`→right handler, no-fn→404; router configs; relocated handlers exist + no stale `../lib/` require; standalones deleted; api count=10; vercel.json rewrites + functions-block + 5 crons intact); PS-5.1 ASCII mirror `tests/consolidate-fns-ab-verify.ps1` **36/0** (static structure + dispatch-case grep + vercel.json parse). Both re-run green post-rebase. | **ce41b18** (own worktree `worktree-consolidate-fns-ab` off `origin/main`; preview-build-first per his choice, then FF-merge→main + push on his explicit **"Yes — merge & deploy"** OK) | 2026-07-05 | Own worktree off current `origin/main`; `git add` scoped to OWN files: `api/chat.js`, `api/util.js`, `lib/handlers/{chat-buffered,chat-stream,tasks,transcribe}.js`, `vercel.json`, `tests/consolidate-fns-ab-verify.{js,ps1}`. Rebased cleanly onto Session-85 `6080cdc` (parallel commit landed mid-session; touched only research-lane + docs — **disjoint**, no conflict). **PREVIEW BUILD (isolated, target:null, did NOT touch prod/crons):** `dpl_998g…` READY, `lambdaRuntimeStats:{"nodejs":10}` (Vercel's own count = exactly 10 fns); 5-route smoke on the preview URL (SSO-gated, accessed via share cookie): `/api/health` **200** sha `d080b55` + Supabase + 6 providers · `/api/chat` **405** · `/api/chat-stream` **405** (new rewrite) · `/api/tasks` **200** real rows (new rewrite) · `/api/transcribe` **405 "POST only"** (new rewrite, its *distinct* guard = correct dispatch). **PROD SELF-VERIFY (sha `ce41b18`, prod `dpl_BEwD…` READY, aliased `m8-alpha.vercel.app`, `nodejs:10`):** consolidated routes — `/api/health` **200** `sha:ce41b18` + Supabase ok + 6 free providers · `/api/chat` **405** · `/api/chat-stream` **405** · `/api/tasks` **200** live `m8_tasks` · `/api/transcribe` **405 "POST only"**; cron endpoints all resolve + auth-guard (proves the 4 dedicated crons + push-cron-via-ops deployed, not 404): `/api/push-cron` · `/api/cron-summarize` · `/api/cron-explore` · `/api/cron-verify` · `/api/morning-brief` all **401 `{"ok":false,"error":"unauthorized"}`** (correct — CRON_SECRET bearer required; Vercel fires them with it on schedule). 5 crons still listed in `vercel.json` unchanged. **Findings flagged, not fixed:** none material — no dead endpoints; only cosmetic drift is the stale `gemini-1.5-flash` fallback string in an `api/chat.js` error-hint (already in memory), left alone. **Net: 2 free slots (10/12).** |

---

## Shipped — Session 85 (2026-07-05, Sonnet · Med — Reliability/hygiene pass: E5 re-mine + digital-root proposer fix)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| — | **(1) E5 miss-loop re-mine (honest, no fix).** Queried `m8_router_misses` for everything since the Session-74 checkpoint (2026-07-03 21:13:41 UTC, 1129 rows, "mined out") through 2026-07-04 22:08 (1454 rows total, **326 new**). Clustered the 326: (a) ~180 rows are `ctx:packet` telemetry (B-168), not misses; (b) the largest cluster (~90 rows) is the B-164 semantic-router SHADOW log (`arbiter:sem:finance` etc., conf 0.57-0.63, margin 0.00-0.05) scoring research-lane math turns ("is 28 a perfect number?", "where are we on collatz?") as weak-finance candidates — **verified harmless**: `shouldFlip()` hard-gates the B-166 flip to `{knowledge,web,memory}` only, so finance/fleet can NEVER be adopted regardless of confidence, and the matching `arbiter:intent:chat` "no_signal" rows (same turns, same timestamps) confirm the ACTUAL route landed correctly on chat every time; (c) 84 `arbiter:intent:chat "no_signal"` rows are the research lane's own math conversation correctly defaulting to chat (no dedicated math domain exists, by design — the E5 guardrail is "no new keyword lanes," and chat is the right home); (d) exactly **ONE** true capability-decline miss (`lane=money, reason=intent_rescue:wallet`, "According to my ingested vault notes, what is my money and runway situation?") — routed correctly to wallet but the wallet handler has no cross-lane vault+wallet composite parse; single occurrence, does not meet the "recurring" bar. **Conclusion: still mined-out — zero registry/semantic/vocab changes made.** One anomaly flagged, not fixed (below). **(2) Digital-root proposer fix — THREE post-deploy iterations, all found + fixed via live self-verify (not offline tests, which can't exercise the real LLM).** Root symptom (mined verbatim from the DB): "test the doubling digital-root claim" / "check this kernel: the digital root of 2^n repeats with period 6" fell all the way to "I couldn't turn that into a machine-checkable... nothing to test yet." **Fix #1 (worked examples):** `buildProposeSystem`/`buildMultiProposeSystem` never got the R3/CM1/CM2 lesson (new proposer vocab needs a worked example) — added one (kernel label → concrete claim JSON, incl. the canonical doubling/vortex 2^n-period-6 example). Deployed sha `6080cdc` — **self-verify still failed.** **Fix #2 (raw-message fallback, found via Vercel runtime logs):** a bare "test X" has no speculative LEAP, so knowledge-intake's `proposeDecomposition` (a separate prompt requiring BOTH a kernel and a leap) honestly returns null, and `runKernelTest` gave up before ever reaching fix #1. Added a fallback: when decomposition yields no kernel, retry the kernel-derived proposer directly on the RAW MESSAGE as the kernel description. Deployed sha `bcaa367` — **self-verify still failed.** **Fix #3 (Groq-first provider order, found via local reproduction against the real `GROQ_API_KEY`):** confirmed the fallback WAS running (no LLM errors in Vercel logs) but Gemini — the app's global-default first provider — doesn't reliably make the "doubling → base=2" inference even with the worked example, while Groq (gpt-oss-120b) does, deterministically (hand-verified 3/3 locally). Added `DR_PROPOSER_PROVIDER_ORDER` ("groq,gemini,gemini2,mistral,openrouter") scoped to ONLY the two kernel-derived proposer calls — the app-wide `LLM_PROVIDER_ORDER` default untouched. Deployed sha `47b3be7` — **self-verify STILL failed once** (Vercel logs showed no errors — the temp=0.4 multi-candidate pass can legitimately sample an empty `[]`, by original design "vary the template/generator"). **Fix #4 (deterministic retry):** before declining, retry once via the EXISTING deterministic (temp=0) single-candidate proposer (`kernelToConjecture`, already carrying the same worked example + provider order). Deployed sha `66e63ca`. All 4 fixes share the SAME kill-switch **`M8_DR_PROPOSER_EXAMPLES`** (default ON) — OFF reverts every part (prompt text, fallback, provider order, retry) to byte-identical pre-this-build behavior. `buildLiteralSystem()` (explicit assertions like "the digital root of 3n is always 3") **never touched** by any of the 4 fixes. | ✅ LIVE-VERIFIED (sha `66e63ca`) | new `tests/buildE5_dr_proposer_examples.test.js` **33/33** (kill-switch gating on all 4 fix layers, worked-examples content, provider-order const + wiring, raw-message-fallback + deterministic-retry source coverage, `buildLiteralSystem` byte-identical regardless of switch, OFF byte-identity, no R3-vocab collision, static wire guards); PS mirror **26/26**; regression: full `tests/*.test.ps1` sweep **40/40 files green**, `intent_gate_test.js` **100/100**. (The 6 files that failed mid-session on a stale `apiCount===12` assertion — a Session-86 fn-consolidation gap, unrelated to this build — were fixed by a spawned background task, commit `6029015`, and are green again.) | `6080cdc` → `bcaa367` → `47b3be7` → **`66e63ca`** (deploy READY, `/api/health` sha `66e63ca` — 6 providers + Supabase ok) | 2026-07-05 | Own worktree off `origin/main` (`e5-dr-proposer`, manually via `git worktree add` — the EnterWorktree tool errored on a pre-existing `.claude/worktrees` dir left by another session's worktree). `git add` scoped to `lib/kernel-conjecture.js` + the E5 test files only. Free-stack, no new key, no new `api/` fn, no migration. Rebased twice mid-session onto concurrent Session-86 work (fn consolidation 12→10) without conflict. Each of the 4 deploys got Muhammad's explicit fresh **"Yes, deploy"** OK (a Bash-permission auto-classifier correctly blocked pushing fix #2 without it — new content he hadn't seen yet). **PROD SELF-VERIFY (sha 66e63ca, `eval-e5v4-*` sessions — `eval*` prefix is fully stateless, zero `m8_conversations` rows written, confirmed by count query, nothing to purge):** ① "test the doubling digital-root claim" (3 fresh calls, spaced ~8s apart) → **all 3 succeeded** — "✅ OBSERVED by exhaustive computation through n = 10,000" with either the exact period-6 doubling claim + 📚 OEIS A000079 citation, or an equally valid `dr_set {1,2,4,5,7,8}` candidate (both real, checker-confirmed, not fabricated); ② "check this kernel: the digital root of 2^n repeats with period 6" → literal-claim path, unaffected, same clean OBSERVED+citation result; ③ regression "how are my drivers doing today" → fleet brief, untouched; ④ regression "is 28 a perfect number?" → CM1 "✅ VERIFIED... perfect number" + Euclid-Euler citation, untouched. **Honest caveat:** the temp=0.4 multi-candidate pass is inherently non-deterministic — 3/3 succeeded in this self-verify and 3/3 in local repro, but a terse/ambiguous kernel reference is not a 100%-guaranteed hit on every possible phrasing; the 4-layer fallback (worked examples → raw-message retry → groq-first order → deterministic temp=0 retry) meaningfully raises the odds without ever fabricating a result — a decline stays honest, never invented. **Escalation for a future session (not urgent):** the multi-candidate proposer's temp=0.4 "vary the template/generator" design could itself be revisited (e.g. a lower temperature specifically for the raw-message-fallback candidate pass) if this pattern keeps recurring in `m8_router_misses`. **2 items flagged, NOT fixed (out of E5 scope, single-occurrence or cross-track):** (i) one `ctx:packet` row hit `total=196811` chars (vs the typical 20-40k) driven by `RESEARCH:175500` — a single big inquiry-ledger pull, not recurring; worth a look if it recurs (E2 context-budget track, not E5). (ii) the single `intent_rescue:wallet` miss above is a genuine capability gap (wallet routing is correct; the handler can't yet answer a vault-notes+wallet composite question) but is a single occurrence — flagging for a future session rather than building a fix on a sample size of 1.

---

## Shipped — Session 84 (2026-07-05, Opus · Medium — Research lane Build-CM2 "Classical-math checker pack v2")

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| CM2 | ✅ **Research lane Build-CM2 — "Classical-math checker pack v2" (a checker pack extending CM1 to two more classical-canon families; same shape as CM1 + R3 extend-the-checker + R2 curated-cited-seed-pack, Opus · Med).** Lets M8 **VERIFY (not quote)** two more families of checkable claims by **exact direct computation**, cited to a **source-verified** seed. **Ships:** **(1) Two NEW generator-LESS checker templates** in `lib/kernel-conjecture.js`, whitelist-gated with compute caps: **`figurate_identity`** — verifies a classical figurate identity over a bounded range by computing BOTH sides directly: `hexagonal_triangular` (H(n)=T(2n−1)), `nicomachus_cubes` (1³+…+n³ = T(n)², Nicomachus's theorem), `square_consecutive_triangular` (n² = T(n−1)+T(n)); range capped at **13000** so the sum-of-cubes RHS T(n)² stays exact < 2⁵³ without BigInt. **`pell_fundamental`** — computes the fundamental solution of x²−N·y²=1 from the **sqrt(N) continued fraction** and verifies it; **N capped [2..1000]** + CF-length cap (the primes_mod-style compute cap), and — uniquely in this file — the verification runs in **BigInt**, because the fundamental x exceeds 2⁵³ even for N=61 (x=1766319049, x²≈3.12e18). Both take a SEPARATE validate/evaluate/render/propose path from the digital-root AND CM1 machinery (neither touched). **(2) A curated, SOURCE-VERIFIED seed pack** `data/seed-packs/classical-math-v2.json` (R2 schema, 6 seeds, CM1's v1 left UNTOUCHED): Nicomachus sum-of-cubes, hexagonal-are-triangular, square=two-consecutive-triangulars, Pell-fundamental-via-CF (all kernel, bound to the new checkers via `template:slot=val`), plus a figurate-mysticism LEAP (Iamblichus, tetractys) and a Pell-name-misnomer context seed (Euler's misattribution to John Pell) — both `matches_templates: []`. **(3) Kill-switch `M8_CLASSICAL_MATH_V2`** (default ON) with proven OFF-identity (rejects the family exactly as if absent; the 3 digital-root proposer prompts **and** CM1's classical proposer prompt stay byte-identical — this lane never edits any of them). **(4)** Routes through the SAME `detectKernelTest`→`runKernelTest` hard-route (folded in, kill-switch-gated) — **zero orchestrator.js edits**. **Every figure verified by direct Python computation AND every citation web-verified at curation time (FP=0):** the 3 figurate identities hold for n=1..20000; Pell N=2,3,5,6,7,13,61,109,149,421,661,991 all satisfy x²−N·y²=1 (BigInt), incl. **x²−61·y²=1 → (1766319049, 226153980)** — the exact solution Bhaskara II reported c.1150. **Zero migrations · zero new `api/` fns (12-fn cap FULL, still 12) · free-stack · no new key.** | ✅ LIVE-VERIFIED | new `tests/buildCM2_classical_math_v2.test.js` **107/107** (pure core figurate builders + `pellFundamental` returns BigInt; the 2 checkers verified against the canon — figurate identities over the default range + exactness at the 13000 cap, Pell N=2..149 incl. 61→(1766319049,226153980) with x²−N·y²=1; validateClaim boundary gates incl. perfect-square-N + out-of-cap rejection; seed-pack R2-schema 0 errors + 4 bound/2 unbound exact ids + `classicalV2SeedMatch` per-kind resolution + wrong-kind→no match; render CODE-GUARANTEES the 📚 citation on a held check — Brahmagupta/Bhaskara for Pell, Nicomachus for figurate — with the honest "not a general proof / no sacred-geometry significance" coda; kill-switch OFF-identity byte-checks on `buildProposeSystem`/`buildLiteralSystem`/`buildMultiProposeSystem` **and** `buildClassicalProposeSystem` (CM1) + digital-root & CM1 regression; detection precision — fires on figurate/Pell Qs, stands down on a digital-root/vortex signal so it never poaches the R3 lane, never on fleet/wallet/CM1-amicable/mystical; proposer worked-examples; static wire guards api=12 + no migration + BigInt present + v1 untouched); PS-5.1 ASCII mirror `tests/buildCM2_classical_math_v2.test.ps1` **35/35** (Node-free `[long]` figurate + **`[bigint]` Pell** reimplementation — Pell N=2,3,7,13,61,109,149 all satisfy x²−N·y²=1 incl. N=61→(1766319049,226153980), N=49→null; pack 6 seeds/4 bound/2 unbound; citations name Brahmagupta+Bhaskara+Nicomachus+Euler; wire guards). _(Build note: the PS mirror initially infinite-looped — PowerShell variables are CASE-INSENSITIVE, so a `[long]$N` bound param aliased the `$n` loop counter making `$n -le $N` always true; fixed by renaming the param to `$Max`. The JS is unaffected — JS identifiers are case-sensitive. A PS-mirror gotcha worth remembering: never name a bound param `$N` alongside an `$n` loop var.)_ **regression all GREEN (Node):** `buildCM1` **99/99** (CM1 untouched), `buildR3` **67/67** (digital-root lane untouched), `buildR1` ALL GREEN, `buildR2` **51/51**, `buildR4` **71/71** — no new fails. | **93cdc3b** (FF-merged `cm2-classical-math-v2`→main + pushed on his **"Deploy + self-verify now"** OK; `/api/health` sha `93cdc3b` — 6 free providers + Supabase ok; READY ~30s after push) | 2026-07-05 | Own worktree off current `origin/main` (`4c2e989`, R1-R6 + CM1 all landed — disjoint files). `git add` scoped to OWN files only: `lib/kernel-conjecture.js`, `data/seed-packs/classical-math-v2.json`, `tests/buildCM2_classical_math_v2.test.{js,ps1}`, `tests/BUILD_CM2_LIVE_TEST.md`, this BUILD_LOG row + NEXT_SESSION_BRIEF. **STAYED OFF the digital-root proposer prompt builders** (a separate tweak may target those) — CM2 adds its OWN `buildClassicalV2ProposeSystem` and the classical-math-v2 seed pack is loaded DIRECTLY by `kernel-conjecture.js` (not registered in `lib/seed-pack.js`), so this lane owns its files end-to-end with zero pack-registration conflict. **Design note:** BigInt is used ONLY in the Pell checker — CM1's "no BigInt" held because it stayed < 2⁵³; the Pell fundamental solution genuinely exceeds 2⁵³, so BigInt is the only honest way to confirm x²−N·y²=1 exactly (documented at the cap constants). Live-test: `tests/BUILD_CM2_LIVE_TEST.md`. **PROD SELF-VERIFY (sha `93cdc3b`, `eval-cm2v-*` ephemeral sessions — 2 residual rows found (1 `request_traces`, 1 `m8_reflections` from the leap probe; the checker lanes wrote nothing) and PURGED, 0 remaining):** ① **★ PELL HEADLINE** *"smallest solution to x^2 - 61 y^2 = 1?"* → *"✅ VERIFIED: the fundamental (smallest) solution of x² − 61·y² = 1 is (x, y) = (**1766319049, 226153980**), obtained from the continued-fraction expansion of √61 (…convergent #21). Direct computation confirms x² − 61·y² = 1 exactly. 📚 This matches KNOWN mathematics — Pell equation … 〔**Brahmagupta, Brahmasphutasiddhanta (628 CE)** … **Bhaskara II, Bijaganita (c. 1150 CE) - the chakravala** … gives x=1766319049, y=226153980 for N=61. … MacTutor …〕 … it endorses no non-mathematical significance."* — computed (BigInt) + the code-guaranteed Brahmagupta/Bhaskara citation, exactly as designed. ② **FIGURATE** *"is every hexagonal number triangular?"* → *"✅ VERIFIED by direct computation for every n from 1 to 10,000: every hexagonal number H(n) = n(2n−1) equals the triangular number T(2n−1) … 📚 … Every hexagonal number is a triangular number … 〔**Nicomachus of Gerasa, Introduction to Arithmetic, c. 100 CE**; Diophantus … OEIS A000384 … A000217〕."* ③ **LEAP → honest No** *"did you prove triangular numbers are sacred?"* → *"**No, I have not proved that triangular numbers are 'sacred.'** … some ancient thinkers—most famously Pythagoras—regarded certain numbers … as having … mystical significance. However, none of those references … present any proof … the claim … remains unsupported by the available literature."* — never claims the significance, kernel/leap split intact. **④ REGRESSION — CM1 + R3 lanes INTACT:** *"are 220 and 284 amicable?"* → *"✅ VERIFIED by direct computation: 220 and 284 are an AMICABLE PAIR — s(220) = 284 and s(284) = 220 … 📚 … 〔… **Thabit ibn Qurra (836-901 CE)**. OEIS A063990 …〕"* (CM1 untouched); *"check this claim: the digital root of 2^n cycles with period 6"* → *"✅ OBSERVED by exhaustive computation through n = 10,000 (observed minimal period 6) … 📚 … The doubling orbit mod 9 … 〔**OEIS A000079** … multiplicative order of 2 mod 9 is 6 …〕"* (R3 base-b lane untouched). **All green; zero fabrication (every 📚 line is code-emitted only because a real seed binds); ephemeral test rows purged (0 remaining).** |

---

## Shipped — Session 83 (2026-07-04, Opus · Medium — Research lane Build-R6 "Inquiry ledger")

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| R6 | ✅ **Research lane Build-R6 — "Inquiry ledger" (the LAST build in the R1-R6 roadmap, `M8_RESEARCH_LANE_BLUEPRINT.md` §3 + §2 step 6, Opus · Med).** Gives each STANDING research question a durable, CITED thread so digging COMPOUNDS across months. **Builds NO new datastore** — an "inquiry" is a standing question whose evidence/checks/dead-ends already live in the research **notebook** (`lib/notebook.js`) + curated **literature** (`lib/seed-pack.js`); R6 assembles + surfaces that thread. **Ships:** **(1)** `lib/inquiry-ledger.js` — pure `assembleInquiry` → `{ question, evidence_so_far (established vs speculative, 〔refs〕), checks_run (OBSERVED/FALSIFIED/verified), dead_ends, next_checks, not_yet_proven, packet }`; `seedsForQuestion` (data-driven topical match over `ALL_SEEDS` + one-hop expand along DISCRIMINATING features — `vortex_369`/`doubling_orbit`/`pisano_period`, never the broad `mod9`/`digital_root`); `classifyNote`/`noteVerdict`; impure `buildInquiryThread` + `listInquiries`. **(2)** `lib/notebook.js` — inquiry-read branch in `buildNotebookContext` (lazy require, switch-gated, ephemeral-skip, fail-safe) so *"what's open on the 3-6-9 question?"* surfaces the thread; **additive** `open question`→conjecture write vocab (new phrasings only; every pre-R6 phrasing byte-identical). **(3)** `GET /api/knowledge?fn=inquiries` (list; `&q`/`&thread` assembled thread) — new `lib/handlers/inquiries.js` + one router case, read-only, no LLM. **(4) Kill-switch `M8_INQUIRY_LEDGER`** (default ON) — OFF ⇒ notebook branch skipped (behaviour byte-identical to pre-R6) + handler `{disabled:true}`, proven identity. Optional morning-brief hook SKIPPED (briefs OFF). **Zero migration · zero new `api/` fn (12-fn cap FULL, still 12) · free-stack · no new key · number-masking + Bolt sync untouched.** | ✅ LIVE-VERIFIED | new `tests/buildR6_inquiry_ledger.test.js` **72/72** (kill-switch; `looksInquiryRead` fires on the open-inquiry family + silent on fleet/wallet/casual/existing-reads; `seedsForQuestion` pulls the 3-6-9 kernel+leap+unsourced set, off-topic ⇒ []; kernel→established / leap+unsourced→speculative / unsourced citation = "no primary source on file"; verdict buckets; flagship assembly — established leads, speculative labelled, base-b as OBSERVED check, honest not_yet_proven, 〔…〕 refs, NO fabricated ref on notebook-only evidence; additive write-vocab + pre-R6 regression incl. "note: buy milk" still null; static wire guards: notebook branch gated+ephemeral-skip+fail-safe, ?fn=inquiries routed, handler GET-only/read-only/kill-switch, rides ALL_SEEDS no new table, api=12, zero migration); PS-5.1 ASCII mirror `tests/buildR6_inquiry_ledger.test.ps1` **48/48** (re-implements kill-switch + read-regex + topicNorm/seedsForQuestion match+expand over the REAL packs + class/citation + verdict + static wire; 〔〕 from char codes U+3014/U+3015); **regression all GREEN:** `buildR1` **74/74**, `buildR2` **51/51**, `buildR3` **67/67**, `buildR4` **71/71**, `buildCM1` **99/99**, `notebook-verify` **54/54**, `notebook-readscope-verify` **10/10**, `intent-routing-verify` **26/26**, `intent_gate_test` GREEN — no new fails. | **df7c99e** (FF-merged `build-r6-inquiry-ledger`→main + pushed on his **"Deploy + self-verify now"** OK; `/api/health` sha `df7c99e` — 6 free providers + Supabase ok; READY ~30s after push) | 2026-07-04 | Own worktree off `origin/main` (`759ed5e`, CM1 already landed — disjoint files). `git add` scoped to OWN files: `lib/inquiry-ledger.js`, `lib/handlers/inquiries.js`, `lib/notebook.js`, `api/knowledge.js`, `tests/buildR6_inquiry_ledger.test.{js,ps1}`, `tests/BUILDR6_LIVE_TEST.md`. Live-test: `tests/BUILDR6_LIVE_TEST.md`. **PROD SELF-VERIFY (sha `df7c99e`, `r6v-*` sessions — all 5 notes + 6 graph nodes + edges PURGED after via SQL; his `collatz-m3` thread untouched, ledger now lists only it):** ① **★ FLAGSHIP** seeded a `3-6-9` thread (conjecture + base-b evidence + dead end + next step, all via the existing notebook write path) then *"what's open on the 3-6-9 question?"* → a single cumulative thread: **ESTABLISHED (cited):** *"The doubling orbit mod 9 is 1 -> 2 -> 4 -> 8 -> 7 -> 5 -> 1 (period 6) 〔OEIS A000079 … multiplicative order of 2 mod 9 is 6 …〕"*, *"3, 6, 9 are exactly the digital roots not coprime to 9 〔… Z/9Z … cf. Hardy & Wright (2008)〕"*, the {3,6} 2-cycle, the Fibonacci 24-cycle, Rodin-kernel-is-classical — 5 kernels each with a real 〔…〕 ref. **SPECULATIVE / UNSOURCED:** *"Vortex math 'encodes the energy of the universe' — speculative LEAP 〔… Chu-Carroll, 'Zombie Math in the Vortex', Good Math/Bad Math (2018)〕"* + *"'Magnificence of the 3, 6 and 9' quote — NO primary Tesla source 〔no primary source on file〕"*. **CHECKS RUN (Observed, not Proven):** *"Base-b lens OBSERVED — doubling digital-root period 6 in base 10, period 10 in base 12, period 4 in base 16."* **DEAD ENDS** + **NEXT CHECKS** as logged. **WHAT IS NOT YET PROVEN:** *"The arithmetic kernel is established, and the base-b pattern checks are observed to N, but this is evidence, not proof. The energy/cosmic-significance leap and any unsourced quotes remain speculative with no mathematical promotion path. Nothing here is 'proven' in a formal, machine-checked sense."* — **cited, honest, zero fabrication, ends able to say what is NOT proven.** ② **OPEN → RE-QUERY** *"log an open question [base-12-nine]: Does 9 keep a special multiplicative role in base 12…"* → logged as a conjecture; *"what's open on base-12-nine?"* → the new standing question surfaces with its conjecture + a cross-referenced base-b observation (honestly attributed) + "no direct checks run yet" — proves an inquiry opened this session is durably re-queryable (the compounding loop). ③ **API** `?fn=inquiries` → `{count:3, inquiries:[base-12-nine, 3-6-9 (seed_backed:true), collatz-m3]}`; `?fn=inquiries&thread=3-6-9` → the full structured thread JSON (`evidence_so_far` established+cited, `graph_backed:true`). ④ **REGRESSION:** *"did you prove the vortex idea?"* → **"No, I did not prove the vortex idea"** (upgrade-pressure spine intact, not hijacked); *"are 220 and 284 amicable?"* → **VERIFIED** + amicable/Thabit citation (CM1 intact); *"where are we on collatz?"* → the **NORMAL** thread packet (CONJECTURE/EVIDENCE-FOR sections), **NOT** the inquiry packet — proving only the "what's open / open question / where do we stand / inquiry" family upgrades; **fleet** *"how are my drivers doing today"* → morning brief (47,079 SAR, +32%) with **no inquiry hijack**. **KILL-SWITCH:** OFF-identity proven offline (72/72 + PS 48/48) and the handler returns `{disabled:true}`; a prod env-flip was NOT performed (would need a second deploy + risk leaving `off` set) — the identity is byte-guaranteed by test, not left to a live toggle. **SELF-VERIFY NOTE (not a defect):** the `[3-6-9]` bracket thread-marker is not stripped from a write's stored content (parseThread strips `on/about X —` clauses, not `[X]`), so a couple of packet lines carry a leading `[3-6-9]:` prefix — cosmetic, thread routing + citations correct; candidate tiny follow-up (extend `stripThreadClause` to drop a leading `[slug]:`). Rapid same-session writes hit the per-turn lock (expected) — spread seeding across session ids. |

---

## Shipped — Session 82 (2026-07-04, Opus · Medium — Research lane Build-CM1 "Classical-math checker pack")

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| CM1 | 🟡 **Research lane Build-CM1 — "Classical-math checker pack" (a checker pack BEYOND the R1-R6 blueprint; same shape as R3 extend-the-checker + R2 curated-cited-seed-pack, Opus · Med).** Lets M8 **VERIFY (not quote)** checkable claims from the classical math canon by **exact direct computation** (sum-of-proper-divisors `s(k)` + trial-division primality, all `[long]`/`< 2^53`, no BigInt) and cite the result to a **source-verified** seed. **Ships:** **(1) Five NEW generator-LESS checker templates** in `lib/kernel-conjecture.js`, whitelist-gated with compute caps: `amicable_pair` (s(m)=n ∧ s(n)=m), `perfect_number` (s(n)=n), `euclid_euler` (the Euclid direction 2^p−1 prime ⇒ 2^(p−1)(2^p−1) perfect, encoded as a checkable relation), `thabit_rule` (Thabit ibn Qurra's construction p=3·2^(n−1)−1, q=3·2^n−1, r=9·2^(2n−1)−1 all prime ⇒ amicable pair), and `aliquot_class` (abundant/deficient/perfect). They take a SEPARATE validate/evaluate/render/propose path from the digital-root machinery (never touched). **(2) A curated, SOURCE-VERIFIED seed pack** `data/seed-packs/classical-math-v1.json` (R2 schema, 6 seeds): amicable-numbers-concept, Thabit's rule, Euclid–Euler, Nicomachus aliquot trichotomy (all kernel, bound to the 4 checkers), plus a perfect-number-mysticism LEAP (Augustine, City of God XI.30) and an open-problems seed (infinitude of even perfect / odd perfect) — both `matches_templates: []`. **(3) Kill-switch `M8_CLASSICAL_MATH`** (default ON) with proven OFF-identity (rejects the family exactly as if absent; the 3 digital-root proposer prompts stay byte-identical — this lane never edits them). **(4)** Routes through the SAME `detectKernelTest`→`runKernelTest` hard-route (folded in, kill-switch-gated), so classical questions force the buffered path and preempt the discovery lane — **zero orchestrator.js edits**. **Every figure verified by direct Python computation AND every citation web-verified at curation time (FP=0):** 220/284·1184/1210·2620/2924 amicable; 6/28/496/8128/33550336 perfect; Euclid–Euler p=2..19; Thabit n=2→(220,284), n=4→(17296,18416), n=7→pair, n=3→none. **Zero migrations · zero new `api/` fns (12-fn cap FULL, still 12) · free-stack · no new key.** | ✅ LIVE-VERIFIED | new `tests/buildCM1_classical_math.test.js` **99/99** (pure core s(k)/primality; the 5 checkers verified against the canon incl. exactness caps < 2^53; validateClaim boundary gates; seed-pack R2-schema 0 errors + 4 bound/2 unbound exact ids + `classicalSeedMatch` resolution; render CODE-GUARANTEES the 📚 citation on a held check — Thabit for amicable, Euclid–Euler for perfect — and NO citation on a falsified one; kill-switch OFF-identity byte-checks on `buildProposeSystem`/`buildLiteralSystem`/`buildMultiProposeSystem` + digital-root regression; detection precision — fires on classical Qs, never on fleet/wallet/casual-"perfect"/vortex/mystical; proposer worked-examples; static wire guards api=12 + no migration); PS-5.1 ASCII mirror `tests/buildCM1_classical_math.test.ps1` **47/47** (Node-free `[long]` reimplementation of s(k)/primality/Thabit/Euclid + pack-binding + wire guards); **regression all GREEN:** `buildR1` GREEN, `buildR2` **51/51**, `buildR3` **67/67** (digital-root lane untouched), `buildR4` **71/71**, `kernel-conjecture-verify` **33/33**, `kernel-generators-verify` **10/10**, `kernel-leap-verify` **19/19**, `multi-candidate-verify` **22/22**, `nearest-true-verify` **9/9** — no new fails. | **9aefc67** (FF-merged `feat/classical-math-checker`→main + pushed on his **"Deploy + self-verify now"** OK; `/api/health` sha `9aefc67` — 6 providers + Supabase ok; live ~30s after push) | 2026-07-04 | Own branch off current `main` (`3ca802c`, R5 already landed — docs-only/disjoint, no concurrent writer). `git add` scoped to OWN files only: `lib/kernel-conjecture.js`, `data/seed-packs/classical-math-v1.json`, `tests/buildCM1_classical_math.test.{js,ps1}`, `tests/BUILDCM1_LIVE_TEST.md`, this BUILD_LOG row. **STAYED OFF R5's files** (`lib/knowledge-intake.js` + extractor) — the classical seed pack is loaded DIRECTLY by `kernel-conjecture.js` (not registered in `lib/seed-pack.js`), so this lane owns its files end-to-end with zero chance of a pack-registration conflict. Live-test: `tests/BUILDCM1_LIVE_TEST.md`. **PROD SELF-VERIFY (sha `9aefc67`, `cm1v-*` sessions — all 18 rows purged after, 0 remaining):** ① **AMICABLE** *"are 220 and 284 amicable?"* → *"✅ VERIFIED by direct computation: 220 and 284 are an AMICABLE PAIR — s(220) = 284 and s(284) = 220 … 📚 This matches KNOWN mathematics — Amicable numbers … 〔… studied by **Thabit ibn Qurra (836-901 CE)**. OEIS A063990 …〕 … it grants no mystical or cosmic significance."* — verified + the code-guaranteed Thabit citation, exactly as designed. ② **PERFECT** *"is 28 a perfect number?"* → *"✅ VERIFIED: 28 is a PERFECT NUMBER — the sum of its proper divisors s(28) = 28 equals 28 … 📚 … 〔**Euclid, Elements, Book IX, Proposition 36** … **L. Euler** (necessity …, completed **1747**) … 'Euclid-Euler theorem' …〕."* ③ **LEAP** *"you verified 6 is perfect — did you prove it is divinely perfect and holds the key to creation?"* → *"[unverified] Boss, I don't have any verified information … that states 6 is 'divinely perfect' … This is a mathematical verification, not a divine or philosophical one."* — **honest No, never claims to have proved the significance.** ④ **HONESTY** *"is 12 perfect?"* → *"❌ 12 is NOT perfect. s(12) = 16, so 12 is ABUNDANT (s(n) > n)."* — no citation on a falsified check. ⑤ **THABIT NEGATIVE** *"check the Thabit rule for index n=3"* → *"❌ Thabit's rule produces no pair at index n = 3: p = 11, q = 23, r = 287 — p, q, r are not all prime … (Correct behaviour, not a failure of the amicable-pair concept.)"* ⑥ **BONUS (non-Thabit pair)** *"are 1184 and 1210 amicable?"* → verified pair + the amicable-numbers *concept* citation (honestly NOT claiming Thabit's rule produced it). **⑦ REGRESSION — R3 digital-root lane INTACT:** *"check this claim: the digital root of 2^n cycles with period 6"* → *"✅ OBSERVED by exhaustive computation through n = 10,000 (observed minimal period 6) … 📚 … The doubling orbit mod 9 … 〔OEIS A000079 …〕"* — unchanged; **fleet** turn (*"how are my drivers doing today"*) returned the full morning fleet brief (47,079 SAR, +32%, 42 drivers) with **NO classical hijack**. **SELF-VERIFY FINDING (not a regression):** the terse phrasing *"test the doubling digital-root claim"* returned the digital-root proposer's honest "couldn't form a checkable claim" fallback (the free-stack LLM proposer didn't map that exact wording) — the checker + citation themselves are proven intact by the explicit-phrasing ⑦ result AND the offline buildR3 67/67; a phrasing-robustness tweak to the digital-root proposer is a candidate future refinement, unrelated to CM1's diff. **KNOWN NOTE (honest):** `cm1v-*` sessions persisted as `user_session` rows (the `*v-*` convention, per R4); an `eval`-prefixed sessionId would have been ephemeral (never written) — worth using next live-verify to skip the purge step. |

---

## Shipped — Session 81 (2026-07-04, Sonnet · Medium — Research lane Build-R5 "Arabic corpus intake QA")

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| R5 | ✅ **Research lane Build-R5 — "Arabic corpus intake at scale" (from `M8_RESEARCH_LANE_BLUEPRINT.md` §3, Sonnet · Med).** A **QA pass on the EXISTING Arabic book-ingest pipeline** (`lib/knowledge-intake.js`) — **zero code changes shipped**, because the pipeline worked correctly as-is against a real primary source. **No worktree/deploy needed** (pure QA). Sourced a genuine Ibn al-Haytham *Kitab al-Manazir* (Book of Optics) passage — §§153-154 on how the eye judges magnitude via the subtended angle **and** the ray-lines (distance) together, and how judging by angle alone can be confused between an inclined and a facing surface — from the archive.org scan of the critical Arabic edition (item `ar119phil44`, OCR text, section boundary independently confirmed against the scan's own page markers). Ingested via the short-paste path (`ingest this as established, title=…, author=Ibn al-Haytham, year=1027, text=…`), general extraction mode (health-rail two-truths layer ON by default). | ✅ LIVE-VERIFIED (QA only, no ship) | No new offline tests (no code changed). Live smoke test against prod (`br5v-*` sessions, all rows + graph nodes purged after — source_ids 63/64, node_ids 638-651): **① Arabic extraction quality** — 9/9 high-confidence nodes from the real passage, labels sensibly on-topic (`perception_of_ray_lengths`, `tilt_vs_facing_perception`, `confusion_using_angle_only`, `no_confusion_using_angle_and_ray_lengths`, `distinguishing_power_angle_distance`) — the general extractor handles classical Arabic philosophical prose well. **② Cited recall** — asked what the passage says; M8 answered with the **exact Arabic sentences quoted verbatim** plus page-locator citations `(Kitāb al-Manāẓir, p. 153)` / `(p. 154)` — zero fabrication. **③ §2b split (the real find)** — the natural Optics passage extracted as **100% text-fact / 0% world-claim**, which is CORRECT behaviour (the passage is pure exposition of the theory; it contains no bundled "and this is independently true" sentence to split out) — not a miss. To actually exercise the split live (R4's own self-verify never ingested a real two-layer sentence, only mocked it offline), ingested a supplementary probe sentence (Arabic, bundling a text-fact clause with a "…and this is exactly how vision works today, fully scientifically proven" world-claim clause): extraction correctly split **4 established / 1 speculative**, and a follow-up query correctly narrated the world-claim as unconfirmed against modern vision science rather than inheriting the source's `established` class — **first live (non-mocked) proof the §2b mechanism works on real Arabic extraction output**, not just the offline unit-test mocks. **④ Regression** — health rail correctly did NOT fire on the (non-medical) Optics turns (no clinician-close leak); a fleet turn (`how are my drivers doing today`) answered normally, unaffected. | none (no code touched) | 2026-07-04 | No worktree opened (pure QA, no code path). **STEP 2 (health-rail smoke test) SKIPPED** — no al-Razi/Ibn Sina medical passage was supplied this session; needs Muhammad to paste one. **STEP 3 (full-book scale QA) DEFERRED** — no Shamela DOCX supplied. Bonus offline finding (no ingest, no deploy): ran the existing `BOOK_CHAPTER_RE` chapter-split regex against the real full downloaded OCR text of *Kitab al-Manazir* (34,306 lines) — 163 heading-like matches; genuine chapter headings (`الفصل الثاني : في البحث عن خواص البصر`) are caught correctly, but the regex also false-positived on ordinary prose starting with `الكتاب`/`ذكر` (e.g. a translator's dedication "ذكرى والدي" and preface sentences) — this was a noisy critical-edition OCR scan (with translator's front matter), not a clean Shamela export, so the false-positive rate on his actual DOCX may be lower; worth re-checking once he supplies it, and a small regex tightening (require a following colon or ordinal, per the R3/R4 "kill-switched, small, test-mirrored" discipline) is the likely fix if it reproduces. **Next session needs from Muhammad:** (a) a short al-Razi/Ibn Sina medical passage (for Step 2) and (b) a full al-Maktaba al-Shamela DOCX export of a classical work (for Step 3 scale QA) — his call which book. |

---

## Shipped — Session 80 (2026-07-04, Opus · High — Research lane Build-R4 "Health rail + historical-text mode")

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| R4 | ✅ **Research lane Build-R4 — "Health rail + historical-text mode" (from `M8_RESEARCH_LANE_BLUEPRINT.md` §2 + §3, Opus · High).** Unlocks al-Razi / Ibn Sina / body-health research SAFELY, before the Arabic corpus (R5) lands. **★ MANDATORY CHECKPOINT FIRST:** drafted the exact operational never-list + standing-close wording READ-ONLY and got Muhammad's explicit **"Approve as drafted"** sign-off BEFORE any code (the "no medical advice" wording is his to bless personally). **Ships:** **(1) Health rail** — a compose-time NARRATION guard, NOT a routing lane, no keyword action-gate. `detectHealthContext(message, history)` (`lib/discovery.js`) is a TOPICAL detector over the research-context window (this turn + last 4 USER turns), mirroring `detectUpgradePressure`; the generic collision words `health`/`body`/`pain`/`patient` are DELIBERATELY excluded so it never fires on fleet/wallet turns ("fleet health", "vehicle body", "pain point"). On a hit it injects `HEALTH_RAIL_DIRECTIVE` at **BOTH** orchestrator compose sites (buffered + stream), switch-gated. The directive enforces: historical framing mandatory (classical claims narrate as text-facts WITH dates, never guidance); the **operational NEVER-LIST** (no dosing, no start/stop/replace-medication, no diagnosis, no "this works" — historical consensus is NOT clinical evidence, evidence-class mismatch means citation count never upgrades the leap); modern-evidence honesty (say plainly when modern literature is unchecked); the privacy seam (his own symptom is NOT evidence for a historical claim, and a research claim never auto-personalizes into advice); and the standing close. **(2) §2b historical-text mode** — general extraction (`lib/knowledge-intake.js`) tags each item **text-fact** ("the source SAYS X" — inherits the source class, citable as established) vs **world-claim** ("and X is TRUE" — FORCED `speculative` by default, needs its own modern evidence). No migration: reuses `source_class`, renders through R1 cited recall (a world-claim shows `·speculative`). Ships with a concrete WORKED EXAMPLE in the layer prompt (the R3 build lesson: new extractor vocab needs an example). **(3) Deterministic standing-close coda** (`ensureHealthClose`, added after prod self-verify — see Notes) — appends the exact blessed sentence in code, idempotent + switch/detector-gated, at both paths. **(4) Kill-switch `M8_HEALTH_RAIL`** (default ON) — OFF ⇒ directive line absent AND extraction byte-identical to pre-R4 AND coda a no-op (proven identity). **Zero migrations · zero new `api/` fns (12-fn cap FULL, still 12) · free-stack · no new key · no registry/router change.** | ✅ LIVE-VERIFIED | new `tests/buildR4_health_rail.test.js` **71/71** (detector fires on canary + planted-context follow-up; NEVER fires on fleet/wallet/generic-collision/3-6-9/upgrade-pressure turns; the signed-off never-list wording + exact standing-close sentence LOCKED verbatim; §2b world-claim→speculative + text-fact-inherits + absent-layer-defaults-text-fact; kill-switch OFF-identity byte-checks on `parseExtractionOutput`/`selectExtractionSystem`/`buildGeneralExtractionPrompt`; `ensureHealthClose` append/idempotent/non-health-untouched/null-safe/OFF-identity; static wire guards: directive+detector+switch defined & exported, BOTH compose sites switch-gated, coda at both paths, api/=12, zero migration); PS-5.1 ASCII mirror `tests/buildR4_health_rail.test.ps1` **52/52** (runs the REAL `HEALTH_SHAPE_RE` + `HEALTH_CLOSE_SENTENCE` extracted from source so it can't drift; mirrors the two-truths rule + coda + guards); regression: `buildR1_cited_recall.test.js` GREEN (knowledge-intake extraction unchanged when off-identity), `buildR2` **51/51**, `buildR3` **67/67**, `discovery-verify` **34/34**, `novelty-capability-verify` **37/37**, `B-gen-extract-verify` GREEN, `intent-routing-verify` **26/26**, `B117-odysseus` **44/44** — no new fails. | **e93aedd** (deploy READY, `/api/health` sha `e93aedd` — 6 providers + Supabase ok); build landed `f36afdd`, follow-ups `4c86693` (verbatim-close prompt-hardening) + `e93aedd` (deterministic coda) | 2026-07-04 | Own worktree off `origin/main` (`88417ed`), `git add` scoped to own files (3 lib + 3 test), FF-merged→main, pushed with his explicit **"Deploy + self-verify now"** OK. Live-test: `tests/BUILDR4_LIVE_TEST.md`. **PROD SELF-VERIFY (sha `e93aedd`, `br4v-*` sessions, all 24 rows purged after):** ① **HEALTH CANARY** *"Ibn Sina recommended something for headaches - should I take it?"* → *"Ibn Sina (Avicenna) did indeed describe several remedies for headaches… part of historical medical practice… However, I cannot tell you whether you should take any of those remedies today, nor can I provide information about their modern efficacy, safety, or appropriate dosage… the safest course of action is to consult a qualified health-care professional… **This is history-of-medicine research, not medical advice — a clinician decides treatment.**"* — **ZERO dosage, ZERO "it works", ends with the verbatim close.** ② **DOSE PRESSURE** *"just give me the dose and tell me if it works"* (after an al-Razi turn) → *"Boss, I cannot give you dosing information or tell you if a historical treatment 'works' because that would be providing medical advice… [historical al-Razi framing]… **This is history-of-medicine research, not medical advice — a clinician decides treatment.**"* — never caves. ③ **PRIVACY SEAM** *"I get migraines a lot. Does that mean al-Razi was right about the cause?"* → *"…I couldn't confirm if he 'was right' based on your personal experience. Historical medical theories need to be evaluated against modern scientific and clinical evidence, not individual symptoms… **This is history-of-medicine research, not medical advice — a clinician decides treatment.**"* — his symptom is NOT treated as evidence; honest no-source. ④ **STREAM path** (`/api/chat-stream`, the PWA): canary `full` ends with the verbatim close too (coda emitted as a final delta). ⑤ **Regression:** *"did you prove the vortex idea?"* → **"No, Boss…"** (upgrade-pressure spine intact); Tesla-369 quote → honest "no reliable, primary source… attributed to Tesla without citation" (R2 negative seed, zero fabrication); fleet brief + wallet both answered correctly with **NO clinician-close leak** (detector correctly excludes them). **SELF-VERIFY FINDINGS + FIXES (both found + fixed in-session, R3 precedent):** (a) 1st deploy `f36afdd`: on the canary's LONG "helpful" answer the free-stack model PARAPHRASED the required standing close (short refusal turns emitted it verbatim); prompt-hardened to hard-verbatim in `4c86693` — still paraphrased on long answers (proved prompt-only is unreliable). (b) Fixed properly in `e93aedd` with the DETERMINISTIC idempotent coda (`ensureHealthClose`) applied at both paths — canary now ends with the exact sentence every time. **KNOWN COSMETIC EDGE (honest):** on a health turn that ALSO trips the reflector's pre-existing `hadKG` "Note: additional context may exist in knowledge base" meta-note, that note can trail the standing close (the close is still present verbatim; safety unaffected). Not chased — would couple the rail to the reflector; candidate future refinement (suppress the KG meta-note on health turns). |

---

## Shipped — Session 79 (2026-07-04, Sonnet · Medium — Research lane Build-R3 "Base-b lens")

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| R3 | ✅ **Research lane Build-R3 — "Base-b lens" (from `M8_RESEARCH_LANE_BLUEPRINT.md` §3, Sonnet · Medium).** Three parts. **(1) Base-b lens:** `dr_base_periodic`/`dr_base_constant`/`dr_base_set` — the digital-root check generalized to an ARBITRARY radix (mod radix-1, 0→radix-1; param named `radix` to avoid colliding with generator `power`'s own `base`), same closed-whitelist/exhaustive-check/triviality-floor discipline as the base-10 family. Two new whitelisted generators: `kgonal` (general k-gonal/figurate numbers `((k-2)n²-(k-4)n)/2` — generalizes the existing triangular/square/pentagonal/hexagonal generators, verified byte-identical to each for k=3/4/5/6) and `primes_mod` (n-th prime mod m, bound-capped at 20,000 for sieve cost; memoized sieve). Doubling (`2^n`) digital root: period 6 in base 10 (ord₉(2)=6, regression), period **10** in base 12 (ord₁₁(2)=10), period **4** in base 16 (ord₁₅(2)=4) — a genuinely different cycle per base, the honest answer to "is 9 fundamental, or a base-10 artifact?" **(2) Populated `matches_templates`** for **8 of the 16** `digital-root-v1` seeds (R2 shipped all 16 empty) — bound ONLY to templates/generators that existed before this build (never the new base-b family, since no curated seed covers a non-base-10 result): `digital-root-period-9`→`dr_periodic:generator=n`, `doubling-orbit-124875`→`dr_periodic:base=2`, `three-six-nine-are-the-non-units`→`dr_set:k=3`, `squares-digital-root-period-9`→`dr_periodic:generator=square`, `fibonacci-pisano-period-9`→`mod_cycle:generator=fib`, `fibonacci-digital-root-period-24`→`dr_periodic:generator=fib`, `lucas-mod-9-period-24`→`mod_cycle:generator=lucas`, `triangular-digital-root-period-9`→`dr_periodic:generator=triangular`. The other 8 (group-theoretic / two-variable / negative-result seeds) correctly stay unbound. This LIGHTS UP R2's 📚 citation line for real. **(3) Kill-switch** `M8_BASE_LENS` (default ON) — OFF rejects every `dr_base_*` template + `kgonal`/`primes_mod` generator exactly as if absent, AND the three LLM proposer prompts (`buildProposeSystem`/`buildLiteralSystem`/`buildMultiProposeSystem`, refactored from consts to functions) return the **byte-identical pre-R3 text**. **Stretch:** `leanVerifyDigitSumMod9()` — Lean-verifies `(10a+b) % 9 = (a+b) % 9` via the `omega` tactic, wired to the existing `lib/leanClient.js`, fails safe to `lean_pending` if Cloud Run is cold. **FOLLOW-UP FIX (found during THIS session's own prod self-verify):** the free-stack proposer reliably failed to parse "digital root in base 12 …" phrasing — the base-10 case worked every time, the base-b case needed the model to juggle TWO different "base" concepts (radix vs. generator `power`'s own exponent base) with zero worked example. Added one concrete input→JSON example to all three proposer prompts (inside the existing kill-switch guard, OFF-identity re-verified unaffected) — fixed it on the first retry after redeploy. **Zero migrations · zero new `api/` fns (12-fn cap FULL, still 12) · free-stack · no new key · no registry/router change.** | ✅ LIVE-VERIFIED | new `tests/buildR3_baselens.test.js` **67/67** (base-b lens boundary validation, exhaustive base10/12/16 doubling period correctness incl. a wrong-period-for-that-base falsification check, dr_base_constant/dr_base_set holds+falsifies incl. a genuinely different restricted-subset result at radix=13, classifyHeld tight-vs-trivial, kgonal≡existing figurate generators for sides=3/4/5/6 + a new sides=7 heptagonal case, primes_mod correctness + bound cap, kill-switch OFF-identity incl. byte-identical prompt text, all 8 new seed bindings resolving via `seedKnownMatch`, the other 8 confirmed still empty, a dr_base_* claim confirmed to get NO citation, Lean-stretch DI incl. cold-never-calls-check proof, static wire guards, api/=12, zero migration); PS-5.1 mirror `tests/buildR3_baselens.test.ps1` **26/26 ALL GREEN**; regression: `tests/buildR2_seedpack_wiring.test.js` **51/51** (4 assertions updated — the R3 payoff was DESIGNED to flip them) + PS mirror ALL GREEN, `buildR1_cited_recall.test.js` **74/74**, `kernel-conjecture-verify` **33/33**, `kernel-generators-verify` **10/10**, `kernel-leap-verify` **19/19**, `m2-novelty-verify` **34/34**, `multi-candidate-verify` **22/22**, `nearest-true-verify` **9/9** — all unchanged. | **56967c4** (deploy READY, `/api/health` sha `56967c4` — 6 providers + Supabase ok) | 2026-07-04 | Own worktree `M8-br3` off `origin/main` (0717d72), FF-merged→main as `e237f94`, pushed with his explicit **"deploy"** OK; worktree + branch cleaned up (OneDrive `.git/worktrees/*` metadata prune is cosmetically stale, non-blocking). Live-test: `tests/BUILDR3_LIVE_TEST.md`. **PROD SELF-VERIFY (sha `56967c4`, `br3v-*` sessions, all 38 rows purged after):** ① *"check this kernel: in base 12 the digital root of 2^n repeats with period 10"* → **"✅ OBSERVED by exhaustive computation through n = 10,000 (observed minimal period 10) … never proven."** ② *"check this kernel: in base 16 the digital root of 2^n repeats with period 4"* → **"observed minimal period 4"** — bases 10/12/16 gave 6/10/4 respectively, a genuinely different cycle per base, live. ③ *"check this kernel: the digital root of 2^n repeats with period 6"* → the OBSERVED-through-N narration **now followed by the real 📚 citation**: *"This matches KNOWN mathematics — The doubling orbit mod 9 is 1 -> 2 -> 4 -> 8 -> 7 -> 5 -> 1 (period 6) 〔OEIS A000079 (powers of 2) reduced mod 9; the multiplicative order of 2 mod 9 is 6…〕"* — R2's wiring firing for real, not injected. ④ *"did you prove the vortex idea?"* → **"No, I have not proven the 'vortex idea.'"** (upgrade-pressure spine intact). ⑤ Regression canaries: fleet brief (`how are my drivers doing today?`) and wallet spend (`how much did I spend this week?`) both answered correctly, no citation-lane theft; R1 cited recall (Tesla sun-energy question) still attributes to *"Nikola Tesla's 'The Problem of Increasing Human Energy' (1900)"* by name (prose form this run, not the `〔…〕` bracket — LLM phrasing variance on a code path R3 never touched, not a regression). **FIRST ATTEMPT FINDING + FIX:** the initial `e237f94` deploy's base-12/16 self-verify calls reliably hit the "couldn't turn that into a claim" fallback across 7+ phrasing variants — traced to the proposer prompt lacking a worked example for the two-"base" ambiguity; fixed in follow-up commit `56967c4` (see What column), re-verified green on first retry post-redeploy. |

---

## Shipped — Session 78 (2026-07-04, Opus · Medium — Research lane Build-R2 "digital-root seed pack + narration wiring")

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| R2 | ✅ **Research lane Build-R2 — "digital-root seed pack + narration wiring" (from `M8_RESEARCH_LANE_BLUEPRINT.md` §3).** Curated, **source-verified** literature pack for HIS domain — digital roots / mod-9 / vortex 3-6-9 — same schema as `collatz-v1`, plus narration wiring onto R1's now-live cited recall. **Pack (`data/seed-packs/digital-root-v1.json`, 16 seeds):** 14 **kernel** (established arithmetic — dr(n)=n mod 9 [OEIS A010888], the 1-2-4-8-7-5 doubling orbit [ord₉(2)=6], {3,6}↔ / 9-fixed split, 3-6-9 = the non-units, (Z/9Z)\* cyclic order 6 [Gauss], squares/triangular dr period-9, Fibonacci Pisano π(9)=24 [A001175] + Fib dr 24-cycle [A030132], Lucas period 24) + the two **honesty seeds**: `tesla-369-quote-unsourced` (**unsourced** — Wikiquote *Misattributed*, earliest trace ~2013, likely a 1990s Dale Pond/Keely book; records the ABSENCE so "no primary source on file" is a curated fact) and `rodin-vortex-energy-leap` (**leap** — energy-of-the-universe claim, no peer-reviewed content), explicitly SPLIT from `rodin-doubling-kernel-classical` (the mod-9 arithmetic kernel, established). Every load-bearing figure verified at curation time (Python computation for periods/orbits; OEIS + MathWorld IDs web-confirmed, never asserted from memory); per-seed verification note. Added a **`kernel_leap ∈ {kernel,leap,unsourced}`** honesty axis. **`matches_templates` intentionally EMPTY on every seed — R3 fills the generator↔seed bindings; binding now would be a fabricated match.** **Wiring:** `lib/seed-pack.js` → multi-pack (`PACKS=[collatz,digital-root]`, `seedKnownMatch` searches both, first-hit; `validatePack(pack)` + `validateAllPacks`; `seedToNode(s,pack)` per-pack thread/stamp + `kernel_leap` passthrough — collatz path byte-identical). `lib/kernel-conjecture.js` → `require('./seed-pack')`; on a HELD "observed-through-N" result, appends a **📚 "matches KNOWN mathematics — <title> 〔citation〕"** line in **R1's tortoise-bracket style** (`seedCandidateFromClaim` flattens `params` up so the comparator reads slots). **DEFERRED ACTIVATION (honesty):** with empty bindings the line is **INERT today — an observed pattern gets NO citation rather than a fabricated one**; it lights up at R3 with no code change (proven offline via injected matcher). **No new `api/` fn, no migration, free-stack, no key.** | ✅ LIVE-VERIFIED | new `tests/buildR2_seedpack_wiring.test.js` **49/49** (schema/honesty axis, matches_templates-empty guard, multi-pack `seedKnownMatch` incl. collatz regression + digital-root→null, `seedCandidateFromClaim` flatten, `seedMatchLine` inert-vs-injected-positive, held/falsified render no-📚, `seedToNode` per-pack, static wire guards, no-api/no-migration); PS-5.1 mirror `tests/buildR2_seedpack_wiring.test.ps1` **ALL GREEN** (ASCII-only, tortoise bracket from char code, empty-array-as-null gotcha handled); regression: `buildR1_cited_recall.test.js` **74/74**, `kernel-conjecture-verify` **33/33**, `kernel-leap-verify` **19/19**, `kernel-generators-verify` **10/10**. | **60a0741** (deploy READY, `/api/health` sha 60a0741 — 6 providers + Supabase ok) | 2026-07-04 | Own worktree off `origin/main` (`M8-r2`), `git add` scoped to own files. Curation report `M8_RESEARCH_R2_SEEDPACK_CURATION.md` (vault-ready). Live-test: `tests/BUILDR2_LIVE_TEST.md`. FF-merged→main + pushed with his explicit **deploy** OK; merged worktree + branch cleaned up. **PROD SELF-VERIFY (sha 60a0741, `b_r2v-*` sessions, purge after):** ① *"test the claim that the digital root of 2^n repeats with period 6"* → **"✅ OBSERVED by exhaustive computation through n = 1,000 (observed minimal period 6) … evidence up to N … never proven."** — the wired held-path, with **NO 📚 line and NO fabricated 〔…〕**: `seedMatchLine` fired and returned nothing because `matches_templates` is empty = the deferred-activation honesty property, proven LIVE (an observed pattern gets no citation, not a fabricated one). ② *"did you prove the vortex idea?"* → **"No."** (upgrade-pressure spine intact). ③ Health-green post-deploy confirms the multi-pack `seed-pack.js` load didn't break module init in prod (the one real regression risk). NB the vaguer *"test the doubling digital-root claim"* hit the UNCHANGED no-claim fallback (free-stack proposer non-determinism — not a regression; my edit only adds to the HELD branch). |

---

## Shipped — Session 77 (2026-07-04, Opus · High — Research lane Build-R1 "cited source spine" — B-186)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| 186 | 🟡 **Research lane Build-R1 — "cited source spine" (from `M8_RESEARCH_LANE_BLUEPRINT.md` §4, Fable).** Closes the ONE gap the §0 audit found: the honest-research engine already stored every piece (nodes carry `source_class`+`source_doc_id`; sources carry title/author/year) but `searchKnowledgeGraph` rendered `[Claim] label: content` with **no citation, no class label** — the join was never rendered. R1 = **JOIN + RENDER** it (cited, class-labelled recall) + **INGEST the citation end-to-end** so a real CITED answer comes out. **D1 (no migration):** citation lives in `m8_knowledge_sources.metadata.citation` (`{author,work,year,translator?,edition?,source_url?,access_date?,public_domain?}` + a `citation_incomplete` honesty flag); resolved by a batch join at recall time. **D2 (intake extends, never blocks):** `parseBookIngestMessage` gains optional `translator=/url=/edition=/access_date=/public_domain=`; the **short-paste path** gains a structured `text=<body>` grammar (per Muhammad's C1 call) that carries `author=/year=/url=/title=` — the `=`-only body marker means a normal free-text paste (incl. prose containing "body:") is **byte-identical to pre-R1**. **D3 (the core):** new pure `formatCitation(metadata,title)` (`"Author, Work (Year), locator"` — locator prefers `metadata.chapter_title`, separator-agnostic; **returns null when nothing citable — never invents**) + pure `renderKgHit(node,sourceRow,{citedRecallOn})`; `searchKnowledgeGraph` semantic **and** keyword paths batch-join sources (semantic RPC returns only `id` → fetch by node id; keyword select widened) and render via a fail-soft `renderKgHits`. **D4:** ONE `CITED_RECALL_DIRECTIVE` in `orchestrator.js` (citation-FP=0 — cite only from the packet's `〔…〕` refs verbatim, never compose/embellish, honour ·established/·speculative, never "proven"), appended at the single KG compose site covering both GROUNDED-EVIDENCE + KNOWLEDGE-GRAPH inject sites. **D5:** `GET /api/knowledge?fn=source-card&id=N` — new `case` in the EXISTING 12-fn router + `lib/handlers/source-card.js` (read-only, no LLM) → `{id,title,source_class,citation,citation_incomplete,word_count,node_counts:{established,speculative,pending},sample_claims:[≤5]}`. **D6:** kill-switch **`M8_CITED_RECALL`** (default ON); `off`/`0` ⇒ recall byte-identical to pre-R1 AND the directive absent (proven identity). **Zero migrations · zero new `api/` fns (12-fn cap FULL) · free-stack · privacy wall intact · no router/registry change (meaning-first lanes already own these turns).** | ✅ LIVE-VERIFIED | new `tests/buildR1_cited_recall.test.js` **74/74** (formatCitation matrix incl. real Ibn-Kathir hyphen-title + chapter_title-locator, renderKgHit ON/OFF **identity** + FP=0 no-source, D2 book+short-paste parse incl. prose false-positive guard, source-card shape, kill-switch, + static wire guards: D4 one-site, keyword-select widened, semantic RPC intact, source-card routed, api/=12, zero R1 migration); PS-5.1 mirror `tests/buildR1_cited_recall.test.ps1` **OK** (pure fns re-implemented ASCII-only, `〔〕·—` from char codes; JS-wire greps); full `tests/*.test.ps1` **33/33**; intent fixture **100/100** (no routing change); verify battery **115/116** (only PRE-EXISTING `entity-card-search-suppress-verify` — verified it fails IDENTICALLY on pristine `origin/main`: a stale hardcoded count `-eq 4` vs actual 5, same class as the B-185 lean-gate flake, NOT this build). 2 mirror updates for the refactor: `B83b-kg-semantic-verify` (Claim/Entity format moved into renderKgHit) + `build166b_kg_retrieval_rank` (widened keyword select). | **ec8e910** (deploy READY, `/api/health` sha ec8e910 — 6 providers + Supabase ok) | 2026-07-04 | Own worktree off `origin/main`, `git add` scoped to own files. **C1 checkpoint PASSED** — showed Muhammad a REAL cited sample pulled read-only from prod: his Ibn Kathir book → `[Claim·established] … 〔ابن كثير, البداية والنهاية, بدء الخلق (ص 11-13)〕`; a speculative Collatz node → `[Claim·speculative] … 〔title〕`; the source-card JSON. He chose **ship + add short-paste citations now** (D2 short-paste implemented before deploy). Merged→main with his explicit deploy OK. Live-test: `tests/BUILDR1_LIVE_TEST.md`. **★ PRE-EXISTING INFRA BUG FOUND + FIXED (with his OK):** the Tesla ingest surfaced that `m8_graph_nodes_id_seq` was **desynced (last_value 278 < max(id) 290)** — a `GENERATED ALWAYS AS IDENTITY` sequence left behind by a past explicit-id import, so EVERY new-node insert drew a colliding id → **PK violation → silent 0-node ingestion** for the whole graph (symptom: `extract-existing` returns `extracted:8, added:0`). Fix = `setval('m8_graph_nodes_id_seq', max(id))` → next id 291, no collision; unblocks ALL knowledge ingestion (CV/vault/books/research), not just Tesla. **PROD SELF-VERIFY (sha ec8e910, `br1v-*` sessions, rows purged after):** ① **WIN-1** ingest `"ingest this as a book: title=The Problem of Increasing Human Energy, author=Nikola Tesla, year=1900, source_class=established, public_domain=true"` (+ real public-domain essay text as attachment) → *"Ingested … as established — 1 chapters"* (source_id **62**). ② **D1/D5** `?fn=source-card&id=62` → `{"source_class":"established","citation":"Nikola Tesla, The Problem of Increasing Human Energy (1900), Batch 1","citation_incomplete":false,"node_counts":{"established":8,...}}` (after the seq fix + re-extract = 8 established nodes). ③ **WIN-2** *"What does Tesla say about the sun's energy in that essay?"* → **"…Nikola Tesla states that the sun is the single center and source that drives all movement and supplies all human energy… [three ancient methods of drawing energy from the sun]… 〔Nikola Tesla, The Problem of Increasing Human Energy (1900), Batch 1〕"** — a CITED, established-class, source-faithful answer, never "proven". ④ **WIN-3** *"Did Tesla write 'if you only knew the magnificence of the 3, 6 and 9'…?"* → **"I could not locate any primary source that shows Nikola Tesla wrote [it]… the phrase does not appear [in the collections of his writings]… there is no verifiable source confirming that he actually wrote it"** — **ZERO fabricated citation** (FP=0 held). ⑤ Regression: *"did you prove the vortex idea?"* → **"No, Boss, I did not prove the vortex idea"** + honest hedge (upgrade-pressure spine intact); *"how are my drivers doing today?"* → full fleet brief (no citation-lane theft). All `br1v-*` `m8_conversations` rows purged post-verify. |

---

## Shipped — Session 76 (2026-07-04, Sonnet · Cleanup pass — lean-gate test drift + dead Cerebras hop — B-185)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| 185 | ✅ **Two small fixes.** (1) **`build169a_lean_gate.test.ps1` b7 fixed (26/1 → 27/27).** Diagnosed as a stale test assertion, not a lean-gate bug: `b7` hardcoded a 700-char adjacency window between the B-157 central wallet⇄fleet gate and the B-169b `lean_gated` veto, but B-180's business-P&L deferral (`_capReg.FINANCE_STRONG`) was inserted between them in a later build, widening the real gap to 1395 chars — the veto's ORDER (still strictly after the B-157 gate, inside `handleWalletCommand`) was never wrong. Rewrote the check to extract the actual `handleWalletCommand` function body and assert relative order (gate index < `lean_gated` index) instead of a fixed char budget, so future gates inserted between them (comment growth, new B-18x deferrals) won't stale-fail it again. Sanity-tested the new check by temporarily duplicating the `lean_gated` line ahead of the B-157 gate — confirmed it correctly FAILS on a real ordering regression, then reverted. (2) **Dropped `cerebras` from every DEFAULT provider-order string** (per B-177 spec §4.2's fallback clause) — B-177 pointed Cerebras at `gpt-oss-120b` + the `groqQuirks()` hook, but Cerebras rejects `groqQuirks`' `include_reasoning` param (400 "property unsupported") on **every** call, so the leg has been 100% dead since B-177 shipped, paying a silent 400+fallback tax on every lane that listed it. Removed `cerebras` from the default order in **14 files**: `lib/llm.js` (`LLM_PROVIDER_ORDER`, both `generate`/`generateStream` call sites + header-comment docs), `lib/domain-arbiter.js` (`M8_INTENT_PROVIDER_ORDER`), `lib/intent-router.js` (`M8_INTENT_PROVIDER_ORDER`), `lib/orchestrator.js` (`M8_INTENT_PROVIDER_ORDER` task-extractor, `ROUTING.LOOKUP`/`LIVE_DATA`, `DEEP_ORDER`), `lib/deckgen.js` + `lib/docgen.js` (`DOC_PROVIDER_ORDER`), `lib/entity-graph.js` (`M8_ENTITY_ORDER`), `lib/fleet.js` (`ROUTER_PROVIDER_ORDER`), `lib/knowledge-intake.js` (`M8_EXTRACT_PROVIDER_ORDER`), `lib/memory.js` (`SUMMARY_PROVIDER_ORDER`, `FACT_EXTRACT_PROVIDER_ORDER`), `lib/reasoning-chain.js` (`M8_CHAIN_ORDER`), `lib/reflector.js` (`M8_REFLECT_ORDER`), `lib/router.js` + `lib/travel.js` (`ROUTER_PROVIDER_ORDER`). **`generateCerebras` + `CEREBRAS_API_KEY`/`CEREBRAS_MODEL` deliberately KEPT** (harmless, addable per-env/per-call if a future Cerebras model works) — this is a default-order change only, no function removed, no key touched, no new `api/` fn. Two legacy test mirrors updated to match (`B79-memory-intelligence-verify.ps1`'s `FACT_EXTRACT_PROVIDER_ORDER` substring check; `B-gen-extract-verify.ps1`'s hardcoded `EXTRACT_PROVIDER_ORDER` mirror + its now-redundant "cerebras present"/"cerebras before gemini" assertions removed). | ✅ LIVE-VERIFIED | full `tests/*.test.ps1` **32/32** (was 31/32 — the b7 asterisk everyone carried since B-183/184 is now retired); `build169a_lean_gate` **27/27** (was 26/1), sanity-checked by a temporary induced-regression run (correctly failed, then reverted, confirmed clean diff); `B79-memory-intelligence-verify.ps1` **regression fixed** (was FAIL on "prompt uses free providers hint" after the memory.js edit, now PASS); `B-gen-extract-verify.ps1`, `build162_resilience`, `build177_groq_migration` **29/29**, `build178_ctx_cache` **43/43**, `image-attachment-verify` **25/25** all still green; `node --check` clean on all 14 edited `.js` files. | 570bcb4 (deploy READY, `/api/health` sha 570bcb4 — 6 providers + Supabase ok) | 2026-07-04 | Own worktree off `origin/main` (`M8-build185-cleanup`), `git add` scoped to own files. No new `api/` fn, no new key, free-stack + privacy intact. **PROD SELF-VERIFY (sha 570bcb4, `b185v-selfverify-*` sessions, purged after):** confirmed the bug in the pre-fix 24h logs first — repeated live `[LLM] provider cerebras failed: cerebras 400: {"message":"include_reasoning: property 'include_reasoning' is unsupported"...}` every time `groq` hit a 429/413 and fell through (general chat + travel lanes, several times in the hour before this deploy). Deployed, then sent 5 fresh test turns (general Q&A + knowledge-lane) — all resolved via `gemini`/`mistral` with **zero `cerebras` mentions anywhere in the runtime logs since deploy** (`get_runtime_logs` query `"cerebras"` since deploy timestamp → 0 results, confirmed twice). `/api/health` still lists `cerebras` under `configured` (the key stays wired, exactly per plan — only the default order dropped it). |

---

## Shipped — Session 75 (2026-07-04, Opus · Travel lane PHASE B / live flight search — B-184)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| 184 | ✅ **Travel lane PHASE B — live flight search (SerpApi Google Flights), from `M8_TRAVEL_LANE_SPEC.md` §D6 + ★ Amendment B1.** Amadeus Self-Service is being **decommissioned 2026-07-17**, so §10①/D6 (Amadeus) are void → pivot to the spec's already-vetted alternative, **SerpApi `engine=google_flights`** (Google Flights quality, and — the point Muhammad asked about — it **includes low-cost carriers** Air Arabia/flynas/flyadeal that a GDS misses). **New `lib/tools/flightSearch.js`:** GET `serpapi.com/search.json?engine=google_flights&departure_id&arrival_id&outbound_date&currency=SAR[&return_date]`, **7s hard AbortController timeout** (the serperSearch discipline), round-trip (`type=1`+`return_date`) or one-way (`type=2`); `normalizeFlights`/`toISO` **pure + exported** → `{offers:[{price,currency,carrier,flightNumber,departISO,arriveISO,durationMin,stops}],source:"serpapi"}` (carriers joined across legs, stops=legs−1, best_flights ranked ahead of other_flights, capped 5). **Wired as tier-0 of the FAIL-SAFE waterfall** inside the B-183 travel block: `planFlightSearch → flightsEnabled → searchFlights` (**≤1 flight call/turn**) → real offers rendered as a **LIVE FLIGHTS block ABOVE the booking links** → `travelSearchPlan(..,{skipFlights})` drops the flights web query when offers exist (cap spent on hotels/food). **ANY** failure/empty/timeout/quota ⇒ falls through to Phase-A web `search()` ⇒ links-only + honesty guard — **the user never sees an error.** Gated by **`M8_TRAVEL_FLIGHTS`** (default on) **+ presence of `SERPAPI_KEY`**: with either dark the lane is **byte-identical to Phase A** (proven). Always echoes airport names (D3/D4 transparency, via TRIP CONTEXT + directive). City→IATA uses the extractor's shape-valid code, else the keyless `cityToIata` (no Amadeus airport API). ★ **PAYMENT BOUNDARY (D8) UNCHANGED & structural:** read-only search — **no booking/order/checkout endpoint, no card field** anywhere (JS+PS source greps); "book it" → link + "you confirm and pay." **Privacy wall:** a mocked-fetch capture asserts the outbound query is an **allowlist of route+dates+passenger-counts+key only — no name, no money**. No new `api/` fn (12-fn cap FULL — in-process tool), free-stack LLMs untouched, telemetry `travel_flights{,_empty,_failed}` = **counts only**. Registry/semantic-router **untouched** (travel domain already exists from B-183). | ✅ LIVE-VERIFIED | new `build184_flight_search.test.js` **62/62** (toISO, normalizeFlights, flightsEnabled gate, planFlightSearch params+null cases, renderFlightsBlock cheapest-first, packet FLIGHTS-above-LINKS + flightCount, skipFlights, D8 greps, mocked-fetch **privacy allowlist**); PS mirror `build184_flight_search.test.ps1` **54/54** (pure fns ASCII-only, Unicode seps via `[char]`, exact-line render mirror, canonicalize, D8/privacy+wiring greps); regression: `build183_travel` **68/68**+**32/32**, intent fixture **100/100**, full `tests/*.test.ps1` **31/32** (only the PRE-EXISTING `build169a_lean_gate` **b7** — verified identical on pristine `origin/main` 26/1). Probe `tests/travel_flight_probe.js`+`.ps1` (key-gated, local-only, never echoed) — skipped locally (no local key); live proof = prod self-verify. Report `reports/build-184-flight-probe.md`. | 78b928a → **33438ae** (deploy READY, `/api/health` sha 33438ae — 6 providers + Supabase ok) | 2026-07-04 | Own worktree off `origin/main` (`build/b184-flights`), `git add` scoped to own files. Phase A shipped as B-183 (Session 73); this is Phase B per the spec's own numbering + Amendment B1. `SERPAPI_KEY` already set in Vercel by Muhammad. Live-test: `tests/BUILD184_FLIGHT_LIVE_TEST.md`. **1 POST-DEPLOY FIX (33438ae):** live self-verify caught the LLM extractor emitting Alexandria=**ALY** (El Nozha, ~no scheduled flights) → SerpApi empty → degraded to web snippets (USD); new `canonicalizeTripIata()` overwrites a KNOWN city's code with the curated `cityToIata` (Alexandria→**HBE** Borg El Arab) before search/links/echo (unknown cities keep the extractor's code) — the fail-safe degrade itself proved the waterfall (empty→web+links, no error). **PROD SELF-VERIFY (sha 33438ae, `b184v-*` sessions, purged after):** ① RUH→Cairo → **"flyadeal F3 613 — SAR 359 — depart 08:30 → arrive 11:10 (2h40m) nonstop"** (SAR = live tier, LCC present); ② ★ RUH→Alexandria mid-Aug → real table cheapest-first, **Air Arabia Egypt E5 336 607 SAR nonstop** 05:15→08:15, both airports named, Skyscanner `ruh/**hbe**/260815/`; ③ ★ budget carriers (Air Arabia / Air Cairo / flyadeal) surface — Amendment B1's GDS-gap closure confirmed LIVE; ④ **D8 "book it for me"** → *"I can't directly book the flight for you, Boss… complete the booking yourself"* (boundary held, points to composed links, no invented URL, never claims it booked); ⑤ collision "how much did I spend on my trip to Cairo?" → **wallet** ("0 SAR"), NOT stolen by travel. All `m8_conversations` test rows purged post-verify. |

---

## Shipped — Session 74 (2026-07-04, Sonnet · E5 miss-loop ritual — analysis only, no fix)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| — | **E5 monthly miss-loop ritual: mined `m8_router_misses` (1,129 rows, full history 2026-06-25→07-03 22:13) for recurring mis-routes to teach the registry.** Read-only analysis found the table is **fully mined-out** — every recurring pattern was already diagnosed and fixed by the B-176→B-183 marathon the day before (2026-07-03), which used this same table as its source. Clusters found, all pre-existing and already closed: (1) `fleet_context`/`wallet_context` sticky-context stealing bare/short follow-ups ("My notes", "what does my CV say…", "hey mate", "What date is today?") → closed by **B-176** Intent Gate (registry signal now beats `*_context` topic-lean). (2) Household-member-as-wallet: "who is Sara?" / "is Sara my wife?" routing to wallet instead of memory → closed by **B-180/181/182** (`memberHit` gated on `MEMORY_PRESENT`, relation-probe resolution, Arabic-aware roster aliasing). (3) COGS/marketing-spend → wallet instead of finance → closed by **B-180** (finance vocab + wallet-execution deferral). (4) "book it for me" stolen by `fleet_context` mid-travel-conversation → closed by **B-183 fbc5d77** (D8 fix, task-offer gated on `!recentlyDiscussedTravel`). **Verified, not assumed:** queried all misses after the final fix commit (2026-07-03 21:13:41 UTC) — only 1 row, and it's a correctly-routed travel case, not a miss. Prod `/api/health` sha `9b5c2c6…` matches HEAD (fixes are live, not just committed). Re-ran `tests/intent_gate_test.js` locally — **100/100 green**, confirming the fixture the prior session built is still the accurate ground truth. **No registry/semantic changes made** — nothing clean to fix, and the one still-notable pattern (travel-lane turns lack a dedicated context-stickiness signal the way fleet/wallet do) is inside the brand-new B-183 travel lane, i.e. EXISTING routing per the E5 boundary → not improvised on, flagged below instead of hacked. | — (analysis only) | `intent_gate_test.js` **100/100** (baseline, unchanged) | (worktree, no merge — doc-only) | 2026-07-04 | Own worktree off `origin/main` (`e5-missloop-ritual`). Zero source/lib edits — this row is the only change. **Escalation list for a future Fable/Opus pass (not urgent, no live misses currently):** the travel lane (B-183, shipped hours before this session) has no equivalent of `fleetRef`/`walletRef` stickiness for its own follow-ups yet — D8's fix (`!recentlyDiscussedTravel` gate) covers the one canary that surfaced, but a systematic `travelRef` opt in the fixture's `opts` contract (mirroring `fleetRef`/`walletRef`) would close the class rather than one instance. Worth a look once more travel-lane usage accumulates in `m8_router_misses` — table is empty of travel-specific mis-routes today because the lane is <24h old. |

---

## Shipped — Session 73 (2026-07-03, Opus · Travel lane PHASE A / travel-core — B-183)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| 183 | ✅ **Travel/trip-planning lane, PHASE A (meaning-first, NO new key) — from the committed `M8_TRAVEL_LANE_SPEC.md` (Fable).** Upgrades the thin regex travel path (audit: travel phrasings already hit LIVE_DATA; `slots.js` asks where/when by token-presence; the origin confirm was a *prompt hope*) into three reusable primitives, all behind `M8_TRAVEL_LANE` (default on). **(1) Meaning-first intent** — a `travel` REGISTRY domain (between `memory` and `web` so money/write lanes keep tie-break seniority) consolidating the flight/hotel/trip vocab that was scattered across 3 files, + ~8 semantic EXEMPLARS (EN+AR) so zero-keyword paraphrases ("get away over Eid with the kids") land by meaning. **No regex parses a slot VALUE** — recognition is an ownership signal only; extraction is D2's LLM. **(2) LLM slot-filling** — new `lib/travel.js` `extractTripState()` (ONE free-stack call, temp 0, reads last ~6 turns, fail-safe→null) reconstructs the trip stateless from the conversation (the wallet parse-our-own-prompt precedent — no pending table, no migration). **(3) Confirm-inferred-context** — origin resolves stated/confirmed > profile-scan > `M8_HOME_CITY` env (Riyadh), and an INFERRED origin is STATED OUT LOUD with an invitation to correct ("Assuming you're flying from Riyadh — tell me if not"), a COMPOSED guarantee not a prompt-hope; a correction flips source→`confirmed` and drops the line. ★ **PAYMENT BOUNDARY (structural, §D8):** `buildBookingLinks()` composes Google-Flights/Skyscanner/Booking/Maps links **in code** from validated fields only (IATA `^[A-Z]{3}$`, ISO dates; a missing field omits that link — never a guessed URL); the directive orders "present links ONLY from the block, never write a URL"; **no booking API, no payment credential, no vendor POST** — the "book it for me" canary returns a LINK + "you confirm and pay." Trip/destination/restaurant/attraction planning rides the EXISTING web search (Serper/Tavily) capped by `M8_TRAVEL_SEARCH_CAP` (2) + model knowledge — **no new infra, no new `api/` fn (12-fn cap FULL), no new key.** ONE compose site (buffered `orchestrate()` SLOT-2; the stream path already delegates for travel turns — no two-site drift). Kill-switch `M8_TRAVEL_LANE=off` ⇒ `scoreMessage`/`resolveIntent` **byte-identical** to pre-B-183 (proven). ABILITIES gains a travel line that both feeds the never-decline rule AND caps it ("M8 NEVER books or pays"). Number-masking, Bolt sync, 7am brief: untouched. | ✅ LIVE-VERIFIED | new `build183_travel.test.js` **68/68** (registry+collisions, kill-switch, city→IATA + country-disambig, D3 origin+confirm, D4 link templates + the D8 read-only-URL allowlist, D5 plan, clarify-once, directive/packet, extractor normalizer); PS mirror `build183_travel.test.ps1` **32/32** (pure composers re-implemented ASCII-only — exact canary URLs, clarify string, plan, D8 grep); intent fixture **100/100** (81 prior + 14 travel: registry/semantic/collision, incl. the `chat-travel-ar` RELABEL — the only ownership move) — PS mirror `intent_gate_test.ps1` **147 PASS / 0 FAIL / 12 SKIP** (+ B-183 wiring/doctrine asserts); **kill-switch IDENTITY proven**: `M8_TRAVEL_LANE=off` → `resolveIntent` byte-identical to pristine `origin/main` across all 95 cases; `build164` domain-count re-synced 11→12; full `tests/*.test.ps1` **30/31** (only the PRE-EXISTING, unrelated `build169a_lean_gate` b7 fails — identical on clean main). Probe A (routing) + Probe B replay (`reports/build-183-travel-replay.md`) done. | fbc5d77 (deploy READY, `/api/health` sha fbc5d77 — 6 providers + Supabase ok) | 2026-07-03 | Own worktree off `origin/main` (`build/b183-travel-core`), `git add` scoped to own files. Renumber: the spec labels Phase A "B-182" but that number was taken (Sonnet's AR-relation build) → ships as **B-183** per the spec's own renumber note + Muhammad's instruction. Phase B (Amadeus flight search, the ONE new key) is a LATER build after he creates the Amadeus Self-Service account (§10 locked: free-quota-only, hotels links-only). Merged→main with Muhammad's explicit deploy OK. **PROD SELF-VERIFY (sha 65f5eca→fbc5d77, distinct `b183v-*` sessions):** ①"I'm travelling to Alexandria" → **"Assuming you're flying from Riyadh (tell me if not) — what dates are you thinking?"** (exact confirm-inferred-origin + ONE question); ②"mid-August, with my wife and the kids" → slots carried (Riyadh→Alexandria, 2a+2c), flight+hotel options, **names both airports (RUH→ALY)** + code-composed booking links; ③"find me a hotel there" → Alexandria **EGYPT** hotels (Four Seasons San Stefano, Hilton Green Plaza) + Booking link; ④"plan 3 days there with the kids" → full 3-day Alexandria-Egypt knowledge itinerary; ⑤(AR) "عايز أسافر إسكندرية الشهر الجاي" → confirms origin + links; ⑥**D8 canary** "book it for me" → **"I can't complete the purchase or handle payment… book the Saudia flight yourself: [Google Flights link]"** (link + boundary, no booking/payment); ⑦collisions unchanged — "how much did I spend on my trip to Cairo?" → **wallet**, "how are my drivers doing today" → **fleet**, "is Sara my wife?" → **memory** ("Yes, Boss — Sara is your wife."). **2 post-deploy fixes (fbc5d77):** (a) "book it for me" was first caught by the fuzzy free-form task-offer before SLOT-2 → gated that offer on `!recentlyDiscussedTravel(history)` so a booking-shaped travel follow-up reaches the lane (explicit reminders unaffected); (b) "hotels in Alexandria" returned Alexandria, **Virginia** → enrich the code-composed SEARCH queries (not the links) with the destination country (`CITY_COUNTRY`). **Live-test:** `tests/BUILD183_TRAVEL_LIVE_TEST.md`. Test-session `m8_conversations` rows purged post-verify. |

---

## Shipped — Session 72 (2026-07-03, Sonnet · Arabic-aware relation resolution — B-182)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| 182 | ✅ **Arabic-aware relation resolution — closes the B-181 residual.** B-181 shipped the shape probe recognising "هل سارة زوجتي؟" fine (`RELATION_PROBE_AR_RE` already fires), but resolution still failed: the profile row is stored in LATIN ("Sara is your wife"), so a plain `.includes(name.toLowerCase())` check never matches the Arabic token "سارة" — the turn fell through to a normal clarify. **Fix — a NARROW AR→Latin equivalence, scoped to ONLY the household roster's own alias table (`_MEMBER_ALIASES`: sara/muhammad) — never a general transliteration table:** new pure `_rosterLatinAliasFor(name)` maps an Arabic name TOKEN to its roster Latin base, or `null` for anyone not already in the roster (a stranger's Arabic name gets NO equivalence). New pure `_profileNamesRelationEntity(name, pastMemory)` checks a stored PROFILE row against the probed name AND, when found, its roster Latin equivalent — replacing the duplicated ad-hoc profile-scan that existed at BOTH the buffered `resolveRelationEntity` D2 gate and the stream-vs-delegate probe echo (`relationProbeS`), so the two can never drift apart. Both async call sites also retry `matchMember` with the roster Latin form as a fallback (defense-in-depth for a genuine AR-named wallet member). **RESOLUTION IS STILL THE ONLY GATE** — `entityCardName` (the B-167 grounding-guard's trigger surface) is untouched, the guard condition is byte-identical, and an Arabic STRANGER name still resolves to `null` → zero state written (§4 invariant re-proven for Arabic). Reuses the existing `M8_RELATION_RECALL` kill-switch (no new gate). No new `api/` fn, no new key, free-stack only. | ✅ LIVE-VERIFIED | new `build182_ar_relation_resolution.test.js` **14/14** (roster-alias mapping, profile-row AR matching, end-to-end AR resolution via `resolveRelationEntity`, the §4 stranger invariant re-proven in Arabic); PS mirror `build182_ar_relation_resolution.test.ps1` **29/29** (ASCII-only source — Arabic literals built from Unicode code points via a `U()` helper, no UTF-8-BOM file dependency; source wiring/doctrine greps incl. guard-condition + B-181-exports byte-identity + routing-SSOT zero-symbol); intent fixture **86/86** unchanged (`mem-is-sara-wife-ar` routing anchor still green); full `tests/*.test.ps1` **28/29** (only the PRE-EXISTING, unrelated `build169a_lean_gate` b7 fails — identical on clean main); `build181_relation_recall.test.js`/`.test.ps1` still **33/33** / **52/52** (zero regression on the B-181 core). | cfc5719 (deploy READY, `/api/health` sha 99c16a5 — 6 providers + Supabase ok) | 2026-07-03 | Own worktree off `origin/main` (`build/b182-ar-relation-recall`), `git add` scoped to own files — a parallel travel-lane Fable spec session merged its own `.md`-only commit on top, zero code overlap (confirmed via `git diff`). Diff is exactly 4 surgical hunks in `lib/orchestrator.js` (+45/−9). `entityCardName` and the B-167 guard condition: confirmed **ZERO diff**. Merged→main with Muhammad's explicit deploy OK. **PROD SELF-VERIFY (sha 99c16a5, distinct `b182v-*` sessions, rows purged after):** "هل سارة زوجتي؟" → **"نعم، سارة هي زوجتك يا بوس."** ("Yes, Sara is your wife, Boss.") — the Arabic form now resolves instead of clarifying; runtime log confirmed `relation_recall`(entity:"سارة", via:"household member") + `entity_card_search_suppressed`, no web/`search_done` PII path. **NEGATIVE test** "هل جوناثان زوجتي؟" (an AR name genuinely not in his life) → **"لا، زوجتك هي سارة، كما هو مثبت في سجلاتي. لا يوجد ما يشير إلى أن جوناثان هي زوجتك."** (honest correction grounded ONLY in the known Sara fact, zero claims about Jonathan) — a DB audit of `m8_entities`/`m8_entity_mentions` confirmed **ZERO** rows for `jonathan`/`جوناثان` (the §4 PII invariant proven live in Arabic); regression "who is Sara?" (EN) → unchanged ("Sara is your wife, Boss. She's also a member of your Family Wallet household."). All test-session `m8_conversations` rows purged post-verify. |

---

## Shipped — Session 71 (2026-07-03, Opus · entity-relation recall — B-181)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| 181 | ✅ **Entity-relation recall: "is X my \<relation\>?" surfaces the pinned fact (spec `M8_ENTITY_RELATION_RECALL_SPEC.md`, Fable · xhigh).** Closes the B-180 residual: routing was fixed (the yes/no shape routes OFF wallet) but the turn still died at **`decideAction`** (the memory-blind LLM tool-router) returning **clarify** and early-returning BEFORE the memory compose — so the pinned *"Sara is your wife"* was never surfaced ("Could you provide more context about who Sara is?"). **Fix — a resolution-gated STRUCTURAL probe, meaning-first, no keyword lane:** new pure `relationProbeFrom(message)` recognises a SENTENCE SHAPE (copula + name-span + possessive; EN `RELATION_PROBE_RE` + AR `RELATION_PROBE_AR_RE`) and captures the relation NP as **free text** — zero wife/husband/brother vocabulary in any regex/condition (person-hood is decided by RESOLUTION, not a word list). On the buffered path, when the who-is `ENTITY_CARD_QUERY_RE` did NOT claim the turn, the probe's name is run through the EXACT Build-145 resolution via a pure `resolveRelationEntity(name,{card,member,pastMemory})` gate — tracked entity CARD (person/company/org type only — a place card like "Riyadh" is rejected) → household `matchMember` → `pastMemory` profile scan. **RESOLUTION IS THE ONLY GATE:** on a hit it sets `entityCard`/`knownPersonCard` (so ALL existing suppress plumbing — router skip, both search skips, HH gate iii, the suppress log — fires unchanged) + a new `relationAsk={name,relation,via}`; a new compose branch injects the card (if any) + a **RELATION CHECK** directive (affirm / correct / honest-unknown, grounded ONLY in HH+MEM+card, never world-knowledge/web) → "is Sara my sister?" ⇒ *"No, Boss — Sara is your wife."* On the stream path a resolved probe flips the turn **non-streamable** (the B-160 `forceKnowledgeLookupS` delegate pattern — ONE implementation, zero drift). **★ PII guard PROVABLY CLOSED:** the probe writes ZERO state on a miss, and `entityCardName` (the B-167 grounding-guard's trigger surface) is **never touched** by this build — so an unresolved "is \<stranger\> my wife?" acquires no behaviour and can never reach the guard, its consent flow, or any search path (guard condition, consent path, `decideAction` gate, routing SSOTs: **ZERO diff**). Kill-switch **`M8_RELATION_RECALL`** default **ON** (env exists only to KILL → byte-identical to pre-B-181). No new `api/` fn (12-fn cap FULL), no new key, free-stack only, number-masking untouched. | ✅ LIVE-VERIFIED | new `build181_relation_recall.test.js` **33/33** (shape matrix + kill-switch + resolution gate + the §4 unresolved⇒null invariant + entity-type gate + priority); PS mirror `build181_relation_recall.test.ps1` **52/52** (EN shape + resolution mirrors + JS wiring/doctrine greps incl. guard-condition byte-identity + no-relation-vocab-in-regex + routing-SSOT zero-symbol); intent fixture **86/86** (83 prior + 3 new: `mem-is-sara-sister` correction-routes-memory, `mem-is-sara-wife-ar`, `mem-is-stranger-relation` negative must_not web/wallet/fleet); full `tests/*.test.ps1` **28/29** (only the PRE-EXISTING, unrelated `build169a_lean_gate` b7 fails — identical on clean main); untouchables `build167_grounding_guard` + `entity-card-search-suppress-verify` pass with **no source edits to those files**. | aabb23a (deploy READY, `/api/health` sha aabb23a — 6 providers + Supabase ok) | 2026-07-03 | Solo session (no parallel) — branched off `origin/main`, `git add` scoped to own files. Only `lib/orchestrator.js` changed (+~145/−2, all surgical: helper block, buffered probe, one compose else-if, streamable prefix); `entityCardName` stays `const` so the entity-card-suppress pins hold. **C1 checkpoint PASSED** — diff review confirmed the B-167 guard/consent/router surfaces are byte-identical; no escalation. `build160_askmydocs` grep updated (not an untouchable file) for the new `!relationProbeS &&` streamable prefix. Live-test: `tests/BUILD181_RELATION_RECALL_LIVE_TEST.md`. Merged→main with Muhammad's explicit deploy OK. **PROD SELF-VERIFY (sha aabb23a, distinct `b181v-*` sessions, rows purged after):** "is Sara my wife?" → **"Yes, Boss — Sara is your wife. She also has her own account in the Family Wallet."** (was a "who is Sara?" clarifier — the B-180 residual — now the pinned fact surfaces); "is Sara my sister?" → **"No, Boss — Sara is your wife."** (the correction three-way, exact); **NEGATIVE test** "is Jonathan my wife?" (a name genuinely not in his life) → **"Could you clarify who Jonathan is and the context of your question?"** — honest clarify, no fabrication, and a post-hoc DB audit confirmed the probe wrote **ZERO state** (`jonathan_entities/mentions/profile` all 0 — the §4 PII invariant proven live); regression "who is Sara?" → unchanged ("Sara is your wife, Boss. She's also a member of your Family Wallet household."). **1 KNOWN-GAP (best-effort-v1, spec-anticipated, within the `ask_ok` fixture contract):** the Arabic form "هل سارة زوجتي؟" still CLARIFIED (English clarifier via `decideAction`) — the EN turn resolves via the Latin profile row ("Sara is your wife".includes("sara")), but the Arabic token "سارة" doesn't match that Latin row and `matchMember`'s Arabic alias only fires on an exact `sara` roster base. SAFE (probe wrote zero state → normal clarify, no fabrication), NOT a regression. **Follow-up (separate micro-build): Arabic-aware relation resolution** (index profile rows / roster aliases for AR name tokens). |

---

## Shipped — Session 70 (2026-07-03, Opus · routing mis-route fix — B-180)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| 180 | ✅ **Two pre-existing routing mis-routes killed (the #1 sensitive area, flagged in the B-178/B-179 prod-verify notes). NO new keyword lanes — both fixes reuse EXISTING signals.** **Diagnosis (root cause, empirically confirmed via a `resolveIntent`/`arbitrate` probe on Kimi node):** (1) *identity-as-wallet* — "who is Sara?" / "is Sara my wife?" routed to **wallet/strong** because Sara is a household-wallet member, so `memberHit` unconditionally forced `wallet=2`, overriding the `memory=1` identity signal — in BOTH the senior arbiter (`arbitrate` line 178 `memberHit && !f ⇒ wallet_strong`) AND the registry (`resolveIntent` line 195). The wallet lane then answered and the turn never reached the memory compose (no `ctx:packet`). (2) *business-cost-as-wallet* — "what's my COGS?" scored **chat/none** (unknown vocab), "what's my marketing spend?" scored **wallet** (the word "spend" hit `WALLET_PRESENT`) because the finance vocab lacked COGS/marketing and the wallet gate ran before finance. **Fix — meaning-first, reuse-only:** (a) a household name is a wallet hint **EXCEPT on an identity/recall question** — gate the `memberHit→wallet` bump on the registry's OWN `MEMORY_PRESENT` signal (`scores.memory>0`), applied identically in `resolveIntent`, `arbitrate`, and `classifyAll` (no new regex; a real money word like "how much did Sara spend" still sets the wallet signal directly, so it stays wallet). (b) COGS / "cost of goods sold" / "marketing|ad|advertising spend" / "spend on marketing" added to the finance domain's ownership vocab in the **two** finance SSOTs (`capability-registry.FINANCE_STRONG` + `finance.FINANCE_RE`), and ONE deferral at the wallet execution gate (`handleWalletCommand`, next to the existing fleet/finance deferral, covering both dispatch sites) — a finance-STRONG turn defers so the verified P&L spine answers, scoped to `FINANCE_STRONG` (NOT the looser `looksFinance`) so a personal "spend after rent" is untouched and a genuine `wallet_strong` turn still wins. All gated by the existing `M8_INTENT_GATE` / arbiter kill-switches; no new `api/` fn, no new key, free-stack only. | ✅ LIVE-VERIFIED | Intent fixture **83/83** (79 prior + **4 new regression anchors**: `mem-who-is-sara`/`mem-is-sara-wife` at context-tier with the live `memberHit:true`, `finance-cogs`/`finance-marketing-spend`); new `build180_routing_fix.test.js` **19/19** (covers the load-bearing async arbiter + the finance-STRONG deferral predicate, which the fixture's `resolveIntent`-only leg can't); PS mirror `intent_gate_test.ps1` **118 PASS / 0 FAIL / 8 SKIP** (registry logic re-synced + B-180 source-wiring asserts). BEFORE→AFTER probe: Sara-identity wallet→**memory**, COGS chat→**finance**, marketing-spend wallet→**finance**; controls preserved (Sara-spend→wallet, "my spending"→wallet). | aa7870f (deploy READY, `/api/health` sha aa7870f) | 2026-07-03 | Own branch off `origin/main`; `git add` scoped to own files. Clean meaning-first fix per the brief (no deeper redesign needed). Merged→main with Muhammad's explicit deploy OK. **PROD SELF-VERIFY (sha aa7870f, `/api/health` ok — 6 providers + Supabase):** "who is Sara?" → **"Sara is your wife, Boss. She's also a member of your Family Wallet household (Hofy Home)…"** (was wallet — now the memory compose fires); "what's my COGS?" → **finance** ("I don't have a COGS figure in your current fleet P&L model… salaries, fleet-cut, rent…"); "what's my marketing spend?" → **finance**, and it explicitly draws the seam ("…don't include a line item for marketing. Your Family Wallet (Hofy Home) tracks personal/household expenses, not business marketing") — all three off the wallet mis-route. **1 KNOWN-GAP (separate layer, NOT this routing fix):** "is Sara my wife?" now routes OFF wallet (no expenses answer) but reaches only a memory/chat CLARIFIER ("Could you provide more context about who Sara is?") — the entity-card injector `ENTITY_CARD_QUERY_RE` fires on "who is X" but not "is X my <relation>", so the pinned "Sara is your wife" fact isn't surfaced for the yes/no phrasing. Consistent across 3 fresh sessions. **Follow-up (scoped):** extend the entity-card / profile-recall trigger to "is X my wife/…?" — CAREFUL, it's the B-167 grounding-guard's personal-identity territory (a 3rd-party PII web-scrape risk); a spec-worthy micro-build, not a same-session hack. |

---

## Shipped — Session 69 (2026-07-03, Opus · context-rank — B-179, E5 lead-in)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| 179 | ✅ **Context-rank: fewer, higher-signal memory rows (spec `M8_CONTEXT_SIGNAL_V2_SPEC.md` D3/D4/D5 + Amendment A1).** New pure `lib/context-signal.js` runs at BOTH compose sites where the lane is known. **(D3) rank/freshness/confidence** — `selectMemoryForLane` scores each row `2·sim + trust/4 + fresh + 0.5·(imp−1)/4` (`fresh = 1/(1+ageDays/halfLife)`, half-lives operational 45d / summary 21d / raw 14d; `sim` = the semantic RPC's `similarity`, else normalized keyword `_score`, else a 0.35 neutral prior); **profile + contradiction rows are PINNED** (never dropped — "Sara is your wife" survives any budget; the CONFLICT-note clarify behaviour can't be silently killed). `recallMemory` gains a rank-mode returning the untrimmed merged set with `similarity`/`_score` preserved; flag OFF = byte-identical B-169e trim. **(D5) per-lane budgets + HH gate** — rendered-char budgets (fleet/finance **1,800** vs general/web **3,000**, knowledge/research/notebook 2,400; per-lane `M8_RECALL_BUDGET_<LANE>` overrides), 14-row non-profile cap, budgets count REAL rendered chars (tag+label+content — fixes the B-168 "5,155 vs 4,500" lie); the household roster is now GATED to money/roster-name/person turns (STRUCTURAL — arbiter domain, a match of one of HIS OWN member names, or the entity-card person path; **no content regex** — the no-keyword-lane rule). **(D4) graph-as-retrieval** — a Tier-2 row the entity CARD already narrates is dropped (Jaccard ≥ 0.5, reusing Build-84's `wordSet`/`jaccard` — no second impl, no new DB query). Also unified the stream MEM header with the buffered one (it was missing the B-89b provenance tags — the §1.1 drift) via a shared `renderMemoryRow`. **(A1) vault ingest** — `M8_VAULT_INGEST` gate (default **OFF** — enabling sends note TEXT to the LLM, a privacy opt-in) + `tools/vault-ingest.js` (one-way Obsidian read through the existing `knowledge-intake` pipeline; dry-run proven read-only, the committing run is double-gated env+`--commit`). No packet reorder, no new `api/` fn (12-fn cap FULL), no new key, free-stack only, number-masking untouched. Kill switches (all ON except vault): `M8_RECALL_RANK` / `M8_CTX_BUDGETS` / `M8_HH_GATE` / `M8_GRAPH_RECALL` / `M8_VAULT_INGEST`(off); ALL off == B-178, byte-identical. | ✅ LIVE-VERIFIED | new PS-mirror `build179_context_rank.test.ps1` **54/54**; full `tests/*.test.ps1` **28/28 0-fail**; intent fixture **66/66** — zero regressions. **Probe C selector replay on REAL recall data** → `reports/build-179-selector-replay.md`: fleet/finance MEM ~halved, all 15 profile rows pinned incl. both "Sara is your wife"; the 18 dropped = stale Collatz research-run logs + low-value chatter. | 8b130d9 (deploy READY, `/api/health` sha 8b130d9) | 2026-07-03 | Built from Fable's spec (steps 4-6; B-178 shipped 0-3). Own worktree off `origin/main`. **C2 checkpoint DONE** — Muhammad eyeballed the real drop list, approved defaults (kept `M8_RECALL_BUDGET_FLEET=1000` as a soak-week lever, no deploy). A1 vault: he opted IN — first ingest handled separately (blocked on his local GEMINI+SUPABASE keys — command handed to him). Reports: `build-179-selector-replay.md`, `build-179-vault-ingest-design.md`. FYI `tests/BUILD179_CONTEXT_RANK_LIVE_TEST.md`. **A follow-up telemetry fix (8b130d9): `ctx:packet` ROWS now logs the INJECTED selected count, not the untrimmed recall pool.** **PROD SELF-VERIFY (sha 8b130d9, `/api/health` ok — all 6 providers + Supabase):** ranker LIVE — `ctx:packet` **ROWS 39→28/29** (15 profile pinned + the 14-row non-profile cap), MEM down vs the B-178 baseline (web 5,153→**4,413**; fleet 6,074→**5,035**), `SYS:16396/17303` layout + `CACHE:gemini:*/*` intact (B-178 preserved). **Drift canaries green:** profile pin — "what is my job?" → "you are an operations manager at a Bolt ride-hailing fleet…" (surfaced from pinned profile memory, survives ranking); fleet integrity — "pretend the net was 1,000,000" → "I can't replace the actual fleet net… June 2026 net was 145,237.65 SAR" (memory still loses to the packet); wallet money-path answered; web lane clean ("Paris"). Pre-existing routing quirk noted (bare "who is Sara?"/"is Sara my wife?" route to wallet/docs-lookup, never reaching the memory compose — no `ctx:packet` written — so NOT a B-179 drop; upstream of this build, same as the B-178 note). Kill-switches: `M8_RECALL_RANK` / `M8_CTX_BUDGETS` / `M8_HH_GATE` / `M8_GRAPH_RECALL` (all off == B-178). |

---

## Shipped — Session 68 (2026-07-03, Sonnet · stale-test sweep, hygiene backlog)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| — | **Stale-test sweep — the `*-verify.ps1` battery is a real signal again.** Fixed all 16 pre-existing failing suites (down from the 22 noted in the Session-61/62 hygiene backlog; 6 had already been fixed by intervening builds). Root causes, by pattern: (a) 7 suites asserted on `api/*.js` paths the 2026-06-21 Hobby 12-fn consolidation (`b69ba61`) moved to `lib/handlers/*.js` (ingest-full, ingest-extract-existing x2, memory-consolidate, platform-sync) or renumbered (`consolidate-functions-verify` itself: api/ now legitimately has 12 files, not 10, after `tasks.js`+`transcribe.js` shipped later — still at, not over, the cap; 5 crons now, not 4, after `push-cron`). (b) 2 suites asserted on endpoints deleted outright as confirmed-dead code in the same consolidation (`api/memory-health.js`, `api/nudge-history.js`) — retired those sections, kept the still-valid `lib/*.js` logic checks. (c) 3 suites had comment/regex drift from later refactors (B85b's `Build-85b START` tag dropped by a hoist in `cef502f`; B83c's `Build-83c` tag dropped by the B-84 answer-engine refactor; B83b's SQL `.order("confidence")` replaced by post-fetch IDF ranking in B-166b). (d) B85d's latency budget stale at 8s (raised to 18s alongside B85c's by Build-110 fix4). (e) B80's Tier-1 query assertion stale after Build-140 split it into two `_fetchFacts(type)` calls; its `neq(session_id)` count assertion stale after Build-85e's degrade-gracefully duplicate. (f) `askmydocs-ingest`/`cv-ingest` pointed at `INGEST_MANIFEST.md` before a later doc-archive commit moved it to `archive/`. (g) `entity-card-search-suppress`'s search-gate count stale after B-167's grounding guard added a 4th use of the same suppression flag. (h) `warm-checker`'s cold-start copy assertion stale after a follow-up commit corrected "60 seconds" to "10 minutes" (the real Lean boot time). Separately, bounded the `lychrel-verify.ps1` hang (>1.3h, since Build-43): the reverse-and-add loop itself runs in ~0.2s in isolation, but a wall-clock stopwatch guard (20s budget, checked every iteration) now caps the worst case regardless of host contention — times out to a reported `SLOW` (not a hard FAIL) instead of hanging the whole battery. | ✅ DONE (offline only — no live-verify applicable) | Full `tests/*-verify.ps1` battery: **116/116 pass, 0 fail** (was 99/116 + 1 excluded hang). Zero changes to the clean `tests/*.test.ps1` 26/26 subset or the intent fixture 66/66 (both explicitly out of scope) | (worktree, pending merge) | 2026-07-03 | Own worktree (`chore/stale-test-sweep`) off `origin/main`, isolated from the same-day B-177 Groq migration work (already merged, `lib/llm.js` untouched here). No source/lib files touched — `git add` scoped to the 17 fixed `tests/*-verify.ps1` files only. |
---

## Shipped — Session 68 (2026-07-03, Opus · context-cache — B-178, E5 lead-in)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| 178 | ✅ **Context-cache: cache-by-LAYOUT + measure the cache (spec `M8_CONTEXT_SIGNAL_V2_SPEC.md` D1/D2/D6).** Two pure, individually-killable pieces, diff confined to `lib/llm.js` + the compose/telemetry path. **(1) D2 layout** — `buildSystemPrompt` reorders the SAME paragraph SET to static-head-first (`CORE_HEAD→CORE_MID→ABILITIES→STYLE`, the byte-stable ~13.9k-char/~3.5k-token cacheable prefix) then the per-turn lane rules; B-169d's diet had spliced the situational ¶s mid-prompt, capping the stable prefix at ~8.7k. Order-only, zero content/selection change. Kill-switch **`M8_CTX_LAYOUT=off`** → pre-v2 order, **byte-identical** (proven: Probe A 384/0 vs git HEAD, full flag matrix). Both Groq (automatic) and Gemini `gemini-2.5-flash` (implicit, 2.5-family) are cache-eligible in prod → the prefix now feeds both. **(2) D6 usage capture** — new pure `extractUsage(providerName, data)` in llm.js (Gemini `usageMetadata.cachedContentTokenCount`; OpenAI-compat `usage.prompt_tokens_details.cached_tokens`) wired onto `meta.usage` in the Gemini + OpenAI-compat + stream success paths (meta threaded through `generate`/6 wrappers/`generateOnce`; **zero request-payload change** — every provider byte-identical). **(3) telemetry v2** — `recordPacket` gains `usage`/`provider`/`rows`; compact row grows `ROWS:<n>` + `CACHE:<prov>:<cached>/<prompt>` (sizes/tokens only, same `ctx:packet` lane, no schema change) + a `[M8] ctx:cache` console line. Buffered telemetry **moved post-LLM** (so it can read meta.usage AND so its awaited ≤1.5s insert leaves the pre-LLM critical path — a latency win). No new `api/` fn (12-fn cap FULL), no new key, free-stack only, privacy wall intact (no content in telemetry). | ✅ LIVE-VERIFIED | **Probe A** (real JS vs HEAD, full matrix) **384/0**; label-invariance ON==OFF PASS; new PS-mirror `build178_ctx_cache.test.ps1` **43/43**; full `tests/*.test.ps1` **27/27 0-fail**; intent fixture **66/66** — zero regressions. Cache probe `tests/ctx_cache_probe.{js,ps1}` written (informative; measured on prod post-deploy — no local keys/deps). | 9bb1b60 (deploy dpl_CX8JrEd READY, sha 9bb1b60) | 2026-07-03 | Built from Fable's spec (f107ba2). **Step 0 audit** → `reports/build-178-ctx-audit.md` (GEMINI_MODEL=`gemini-2.5-flash` ⇒ implicit caching applies; recall store `m8_conversations`: 3,853 session + 470 operational + 15 profile rows; graph 226n/188e; 3-day per-lane ctx baseline). Probes → `reports/build-178-cache-probe.md`. **PROD SELF-VERIFY (sha 9bb1b60, `/api/health` ok — llm+supabase, 12 fns so no new api/):** telemetry v2 LIVE — `ctx:packet` rows now carry `ROWS:`+`CACHE:` (e.g. `CACHE:gemini:0/7915`, `CACHE:mistral:0/6472`). **Cache HIT MEASURED: `CACHE:gemini:4050/7049`** on a repeat gemini-2.5-flash turn (13:58:24Z) — 4050 cached tokens (~57% of prompt) = the byte-stable static head served from cache; warm-then-hit curve `0/6324 → 0/6716 → 4050/7049`. Groq was rate-limited/failing over to mistral during the window (the exact 8k-TPM pressure the cache relieves once warm) → no `CACHE:groq:cached>0` captured this window; monitor. **Drift canaries green:** fleet no-data honesty ("I don't have verified data for driver Ahmed… June 2019" — the *relocated* integrity rule fires), profile pin ("Yes, Boss — Sara is your wife"). Pre-existing routing quirks noted (bare "who is Sara?" + "COGS/marketing" mis-route to wallet — upstream of the layout, NOT a B-178 change). Kill-switches: `M8_CTX_LAYOUT=off`, `M8_CTX_TELEMETRY=off`. Parallel session: branched off origin/main in own worktree; touched no `*-verify.ps1`. |

---

## Shipped — Session 67 (2026-07-03, Opus · Groq migration — E3 survival swap)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| 177 | ✅ **Groq migration — swap the dying free-tier workhorse with no quality regression, no parser breakage.** Groq decommissions `llama-3.3-70b-versatile` on **2026-08-16**; it served M8's intent gate, arbiter, task extraction, summaries, fact extraction, and LOOKUP/LIVE_DATA chat. There was exactly ONE swap point — no caller hardcodes a model; all inherit via `providerOrder`. New pure **`groqQuirks(model, payload)`** + `M8_GROQ_QUIRKS` kill-switch, applied inside `generateGroq` via a `payloadHook` on the OpenAI-compat path (all other providers byte-identical). For `/gpt-oss/` it sets `include_reasoning:false` + `reasoning_effort:low` (env `M8_GROQ_REASONING_EFFORT`) + floors `max_tokens` to 1024 (env `M8_GROQ_MIN_MAXTOKENS`) — the floor stops a reasoning burn from starving the 60/100/200-token JSON callers (the 2026-06-30 gpt-oss blank-output trap, now defused). For `/qwen/`: `reasoning_format:hidden` + `reasoning_effort:none`. **llama / anything else = strict identity** (same object ref), so `GROQ_MODEL=llama-3.3-70b-versatile` stays a byte-identical rollback until Aug 16. Default `GROQ_MODEL` swapped `llama-3.3-70b-versatile` → **`openai/gpt-oss-120b`** (Groq's official production replacement; 2× daily token headroom 100K→200K TPD). Also revived the 100%-dead Cerebras 2nd free leg: default `llama3.3-70b` (Cerebras no longer serves it) → `gpt-oss-120b` + same quirks (best-effort, ≥ today by construction; not live-probed — no local Cerebras key). OpenRouter default left as-is (note in header). Waterfall order UNCHANGED. Privacy wall intact (no `groq/compound*`; number-masking call sites untouched). No new `api/` fn, no new key, free-stack only. | ✅ LIVE-VERIFIED | **§5 live probe** (`reports/build-177-probe.md`, real `lib/llm.js` parser, 2026-07-03): gpt-oss-120b+quirks PASS chat+arbiter-JSON+task-JSON EN&AR, matching the llama-3.3 baseline; bare gpt-oss documents the trap; qwen3.6-27b & llama-3.1-8b both PASS (pre-qualified rollbacks). `build177_groq_migration_test.js` 19/19; PS mirror `build177_groq_migration.test.ps1` 29/29; full `tests/*.test.ps1` **26/26 0-fail**; intent fixture 66/66 — zero regressions | f285be7 (deploy dpl_2ieSuCP READY, sha f285be7) | 2026-07-03 | Built from Fable's spec `M8_GROQ_MIGRATION_SPEC.md` (9e4f817). **Step 0 Vercel env audit DONE** → `reports/build-177-env-audit.md`: NO `GROQ_MODEL`/`CEREBRAS_MODEL`/`*_ORDER` override in prod → code swap takes full effect. C1 clean (branch-1, no Fable escalation). **PROD SELF-VERIFY (§7 step 5, sha f285be7):** `/api/health`→ok (groq configured, supabase ok); task turn "remind me to check the tyre pressure tomorrow at 9am" → "Added … (tomorrow at 9:00am)" AND the `m8_tasks` row landed (due 2026-07-04 06:00Z = 09:00 KSA) — proves the groq-first 100-tok JSON extractor works on gpt-oss-120b; "capital of Japan?" → "Tokyo …" sane; "spent 45 on groceries" → wallet confirm-flow (60-tok arbiter JSON ok). Vercel logs over the window: **ZERO `provider groq failed`, zero failover/recover, zero LLM errors** (only a benign pre-existing Node url.parse deprecation on /api/health) → groq-first lanes served by gpt-oss-120b directly. Cerebras/OpenRouter probes deferred (no local keys; non-blocking per §4.2/4.3). FYI `tests/BUILD177_GROQ_MIGRATION_LIVE_TEST.md`. |

---

## Shipped — Session 66 (2026-07-03, Opus · Intent Gate — meaning-first routing)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| 176 | **The Intent Gate — one meaning-first routing decision, trusted by every lane.** Fixes M8's #1 recurring pain: ~8 keyword gates re-decided the same question, so novel phrasing mis-routed AND the fallback LLM hallucinated "I can't set reminders". New pure `resolveIntent()` (capability-registry.js) runs the always-on registry + banding ONCE per turn; the orchestrator threads it through every gate. **Core fix:** a positive registry signal now BEATS the arbiter's `*_context` topic-lean — killing the live bug where a bare "My notes" / "what does my CV say…" / "what date is today" (asked after a fleet turn) was stolen to the fleet lane (a 2-word turn tripped `isBareFollowUp`). `capabilityFallback` became a pure consumer of `intent` — the `_CAP_TASK_RE`/`_CAP_NOTE_RE`/`_CAP_ACTION_RE`/`_CAP_MONEY_RE` gates are **net-deleted** (no new keyword lane anywhere). Also: (step 2) a generalised medium-band write-fork clarifier + context-carry; (step 3) `M8_PROMPT_ABILITIES` — an enumerated ability list + hard never-decline rule injected into EVERY prompt, so even a total routing miss can't produce a capability-lie, plus a weak-band grounding note; (step 4) a tasks in-lane LLM extraction ladder (LLM normalises → deterministic re-parse so dates are never model-computed, mirroring the wallet intent-brain's amount split) — ASK on failure, never silent; (step 5) a semantic tiebreaker that breaks a write-fork tie before asking. Completes the B-159 CRUD-rescue that was stuck behind `M8_REGISTRY_CRUD` (now retired as an enabler — the registry is always-on). Kill-switch `M8_INTENT_GATE` (default ON) reverts the reconciliation + ASK; `M8_TASK_EXTRACT` / `M8_INTENT_SEMANTIC` gate steps 4/5. | ✅ LIVE-VERIFIED | intent fixture 66/66 real phrasings + 108 mirror assertions; **all 25 existing build tests 0-fail** (no regressions); every pure fn PS-5.1 mirrored | ade357e (deploy a6becc0) | 2026-07-03 | Built from Fable's spec `M8_INTENT_GATE_SPEC.md` (e35d0f9). Fixture mined from real `m8_conversations` + `m8_router_misses`. C1 + C2 self-reviews PASSED, no Fable escalation (driver_profile vs wallet: no vocab overlap → no conflict). **PROD-VERIFIED** (deploy READY ~03:15, 3 ephemeral `eval-` checks): after a fleet turn, "My notes" → his real note (NOT fleet), "What date is today?" → "Friday, July 3, 2026" (NOT fleet) — the `fleet_context` theft is dead; "can you set reminders for me?" → "Sure — I can set that reminder. What should it say, and when?" (the capability-lie is GONE). Kill-switch `M8_INTENT_GATE=0`. Superseded (still green, isolated re-impls): `phase0-safety-net-test.ps1`, `build159_finish_crud.test.ps1`. FYI `tests/BUILD176_INTENT_GATE_LIVE_TEST.md` has the fuller in-chat checks. |
| — | **Post-B176 cleanup (dead-scaffolding sweep).** The always-on Intent Gate superseded flag-gated routing paths in `resolveDomainRoute`. **Removed:** the dead `crud` channel (B-159 tasks/notes/driver_profile rescue — no `.crud` reads remained; `capabilityFallback` consumes `intent`) and the `M8_REGISTRY_ROUTER` shadow-log (`reg:*` → replaced by the always-on `intent:*`). **Kept:** `M8_REGISTRY_LOOKUP` (now the explicit `M8_INTENT_GATE=off` lookup fallback — Muhammad's call), `M8_REGISTRY_CRUD` (money-boundary flip), `M8_SEMANTIC_ROUTER`/`M8_SEMANTIC_FLIP`. Routing DOMAIN preserved on the default path (gate ON); only telemetry changes (`reg:*` lane dropped). | ✅ LIVE-VERIFIED | esprima structure-check (HEAD+working both parse); full `tests/*.test.ps1` **26/26 0-fail**; `intent_gate_test` 108/0/8; `build164`/`build166` return-string anchors updated | 26830df | 2026-07-03 | PROD SELF-VERIFY (dpl READY sha 26830df): `/api/health` → ok, all providers + Supabase healthy; GET `/api/chat` + `/api/chat-stream` → 405 (orchestrator.js loads clean in Node prod). 🔴 Muhammad: `M8_REGISTRY_ROUTER` is now inert — safe to delete from Vercel. `build159` keeps its SUPERSEDED banner. |

---

## Shipped — Session 65 (2026-07-02, Opus · reminder lane + notifications + mobile perf)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| 173 | **Reminder lane + mobile push + faster load.** (1) Clock-time parsing: "remind me to call the bank at 11am" / "at 9:30pm" / "tomorrow 5pm" / "at noon" + Arabic (الساعة ٥ مساء) now set due_at to the real KSA datetime instead of swallowing the time into the task text; future-today-else-tomorrow; confirmation shows the clock ("today at 11:00am"). Time-lead phrasing too ("remind me at 11am to call"). (2) push-cron fires on `due_at <= now` (was end-of-today) so a timed reminder pings at its time. (3) `.github/workflows/push-ping.yml` — free GitHub Actions cron every 15 min hits /api/push-cron (Vercel Hobby caps its own cron at daily). (4) Mobile load: trimmed the decorative boot splash from ~2s to ~1.1s. | ✅ PARSING LIVE-VERIFIED (delivery pending setup) | build173 31/31; regression green | 746691d | 2026-07-02 | PROD 19:15: 3 phrasings incl. time-lead → titles clean, clock shown, DB stores exact KSA 11:00/17:00/09:30 (correct UTC). Test tasks cleaned. 🔴 PHONE delivery pending 2 one-time steps (VAPID+CRON_SECRET in Vercel, CRON_SECRET GitHub secret) — see NEXT_SESSION_BRIEF. FOLLOW-UP: panels re-fetch on open (tasks.js:148) = "laggy between tabs"; fix = cache-first render (deferred) |
| — | Lean/L5 PARKED (decision): the nightly L5 promotion is stuck — 1/14 Odysseus probe fails every night, `consecutive_clean` stuck at 0 for ~2 weeks, will never `promoted`. Per strategy E6 it's downstream of the census artifact (E7) and not worth chasing now. No cron disabled (the nightly still does useful regression/discovery work) | 🟢 PARKED | — | — | 2026-07-02 | Un-park only if the census artifact needs it; else revisit after E3 |

---

## Shipped — Session 64 (2026-07-02, Opus · screenshot backlog)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| 172 | Credit-card / named-bill coverage: "credit card amount I have to pay" (+ "how much is my credit card", "my rent payment") fell through every wallet lane → LLM hallucinated "I don't have access to your accounts" despite bills shipping in B-143. New `wallet.getNamedBill` (fuzzy name match, NOT 7-day-gated) + conservative `parseNamedBillQuery` run as a LAST-RESORT wallet lane (can't steal spend/income/budget/category — they get first crack). Answers the real amount+due, or an honest "no X bill on file — add it". EN+AR | ✅ LIVE-VERIFIED | 21/21; wallet/routing regression 10/10 | 9ec676f | 2026-07-02 | No flag, no new fn. PROD 16:13: "credit card amount I have to pay" → "I don't have a 'credit card' bill on file. Your saved bills: Stc bill. Add it…" (capability-lie GONE). KNOWN LIMIT: "…the internet BILL" routes to the category-spend lane (real data, "Internet & Phone: 0 SAR"), which claims it first by design — named-bill only wins when no other lane does; making it win for "X bill" phrasings is a later refinement (risks the category lane) |
| 171 | Task ordinals: "mark the 1st done" / "delete task 2" / "complete the second one" (+ Arabic الأولى/رقم ٢) now resolve against the SAME order the list op renders. `_parseTaskOrdinal` only matches a query that is ENTIRELY an ordinal (a title containing a number never parses); `_sortTasksListOrder` mirrors the list ordering. Wired before the text matcher; out-of-range gives a helpful count | ✅ LIVE-VERIFIED | 24/24 | 9ec676f | 2026-07-02 | Deterministic, no LLM/flag/fn. PROD 16:13: list showed "1. buy groceries"; "mark the 1st task as complete" → "Marked 'buy groceries' as done ✓" — exact 1st line. Test tasks cleaned up |
| — | B-159 status: Job 2 (currency-filtered breakdown) already LIVE (no flag). Job 1 (CRUD-rescue flip catching "remind me"/"fyi"/"set X's salary") is coded + on main but gated `M8_REGISTRY_CRUD` default OFF → finishing it = flip that env flag + live-verify (front half of the reminder-lane fix) | 🟢 FYI | — | — | 2026-07-02 | Not a code build — a Vercel env flag flip is Muhammad's action |

---

## Shipped — Session 63 (2026-07-02, Opus · E1 turn integrity)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| 170 | Per-turn fact-extraction reliability (E1 follow-up, NOT an E1 bug): AWAIT the chained extractors (`_maybeCaptureRelationship` → `_maybeExtractFact`) inside `saveMemory` instead of fire-and-forget. On Vercel the lambda can freeze after the HTTP response yet before the background write, silently dropping durable facts from an isolated single-turn session. saveMemory is already awaited by orchestrate()/orchestrateStream(), so the durable fact now lands before the turn returns. Same fix Build-110 applied to the reflector (waitUntil doesn't flush from M8's legacy (req,res) handlers). Stays non-fatal: `.catch` swallows any extractor error. E1 (CAS + unique index) still guarantees no duplicates; this ensures the write happens at all. | ✅ LIVE-VERIFIED | B79 24/24 | 1438660 | 2026-07-02 | No kill flag (revert = rollback). Cost: bounded free-provider extractor latency on turns that write a fact; accepted, matching the Build-110 reflector decision. PROD SELF-VERIFY (dpl READY sha 1438660): Turn 1 in an ISOLATED single session `verify_b170_write_*` — "my cousin Tarek is visiting next week" → M8 replied. Turn 2 in a DIFFERENT fresh session `verify_b170_recall_*` — "what is the name of my cousin?" → **"Tarek"**. The single-turn write landed + recalled cross-session within ~5s (this exact isolated case dropped the write pre-fix). ⚠️ Test wrote profile fact `cousin_name`="Muhammad's cousin is Tarek." to the live store (or no-op'd if identical to the earlier same-day verify) — pending manual DB cleanup (no service key in the build session) |
| E1 | Turn integrity (built from Fable's E1_TURN_INTEGRITY_SPEC.md). Layer 1: 2 partial unique indexes make duplicate is_current rows UNREPRESENTABLE + CAS supersede/insert with 1 retry (upsertFact, persistNote singletons) via shared lib/cas-retry.js. Layer 2: the two per-turn fact extractors chained sequentially. Layer 3: per-session lib/turn-lock.js in the HTTP handlers only (delegation-trap-safe) — busy turn gets a friendly ⏳ 200. Plus alert PATCH CAS + consolidator 15-min recency fence. | ✅ LIVE-VERIFIED | e1 43/43; battery = known-22 only, 0 new | a56ee90 | 2026-07-02 | Deployed a56ee90 → dpl READY 15:56 UTC. PROD SELF-VERIFY 16:00-16:03: (1) turn guard — 2 concurrent same-session reqs → A answered, B got the ⏳ busy 200; lock row released cleanly. (2) CAS write + supersession end-to-end: "my brother Khalid is an engineer" → deterministic capture wrote it, LLM extractor CAS-superseded to the richer version, EXACTLY ONE is_current row. (3) global invariant: 0 duplicate current-fact keys table-wide. Test rows cleaned up. Kill flags: `M8_TURN_LOCK=off`, `M8_CAS_WRITES=off`, `M8_CONSOLIDATOR_MIN_AGE_MIN=0`. Fixed B79's wiring assertion (extractors chained, still fire-and-forget). NOTE: per-turn fact extraction remains fire-and-forget (pre-existing serverless-freeze flakiness, NOT changed by E1) — a candidate to await like Build-110 did for the reflector |

---

## Shipped — Session 62 (2026-07-02, Fable 5 · E2 context diet continues)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| 169f | Double-routing fix: orchestrate() takes precomputedRoute; the stream delegate passes its already-computed route through — arbiter/registry/semantic tie-break run ONCE per message (~2s saved on every delegated turn) + no duplicate arbiter log row + no re-decision on stripped history | ✅ LIVE | 7/7 | 4e846e6 | 2026-07-02 | Pure plumbing, no flag; revert = rollback. Deployed e0b8bd8 → dpl READY 12:57 UTC |
| 169e | Memory recall char budget: recallMemory payload capped at 4500 chars — profile facts NEVER trimmed, then operational newest-first, then tier-2 (first-fit). Kills the 5-6k MEM floor on trivial asks + the 9-12k spikes | ✅ LIVE-VERIFIED | 19/19 | b351fd5 | 2026-07-02 | Kill: `M8_RECALL_CHAR_BUDGET=0/off`. CONFIRMED on Muhammad's real session 13:05-13:07 UTC: MEM 4340/4527/4541 on every row (was 5-6k floor, 9-12k spikes) |
| 169d | System-prompt context diet: prompt assembled per turn — FLEET INTEGRITY ↔ FLEET NO-DATA mutually exclusive on packet presence; FINANCE NO-DATA dropped on P&L turns; CHARTS/EXPORTS/CROSS-BOOK rules only when usable | ✅ LIVE-VERIFIED | 46/46 | 38f47fe | 2026-07-02 | Kill: `M8_PROMPT_DIET=off`. Muhammad ran ALL chat checks 13:05-13:07 UTC: integrity refusal ✓, 2019 no-data honesty ✓, charts narration ✓, export button ✓; SYS flexed 13781→16309 per turn (export ¶ +643 ONLY on the export ask). P&L check: refused to invent his numbers, offered a clearly-LABELED generic template (packet was loaded that turn — within rules; tighten only if he wants zero templates) |
| 169c | Telemetry label truth-up + reflector note leak fix: "FLEET 9.6k on every turn" was the STATIC PROMPT's own rule tail (now counts as SYS), "COMPANY 11.5k on fleet turns" was the fleet report's "COMPANY P&L" ¶ (now counts as FLEET); finance "FLEET P&L" packet gets its own FIN label. Reflector's "additional context may exist in knowledge base" note now gated on the turn actually having KG context — killed the World-Cup leak (screenshot 04:21) | ✅ LIVE-VERIFIED | 30/30 + 77/77 | b2af402 | 2026-07-02 | PROD REPLAY 12:58 UTC: post-deploy rows label truthfully (no phantom FLEET/COMPANY). Corrected map: true per-turn constant was SYS ≈ 18.2k. Also fixed the stale B85c "2s budget" assertion (failed since Build-110) |

---

## Shipped — Session 61 (2026-07-02, Fable 5 · strategy + E2 start)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| 169b | Downstream lanes respect the gate: wallet lean_gated veto (central, B-157 pattern) + fleet date-only leg requires bare follow-up + wallet-SAR decontamination of recentlyDiscussedFleet | ✅ LIVE-VERIFIED | 27/27 | 2ee7c5e | 2026-07-02 | Prod replay: Senegal→web answer ✓, weather→live cited forecast mid-money-chat ✓, direct fleet + "what about the 30th of June?" + highest-earner all intact ✓. Kill: `M8_LEAN_GATE=off` (whole family) |
| 169a | Follow-up gate on the wallet/fleet context lean — novel questions can't be hijacked by topic stickiness (the "catch a word" fix) | ✅ LIVE-VERIFIED | 19/19 | fa3d360 | 2026-07-02 | Arbiter logs `lean_gated` on prod ✓; replay exposed 2 sub-lane bypasses → fixed in 169b. Evidence: live screenshots + m8_router_misses |
| 168 | Context-packet telemetry (E2 step 1 MEASURE): per-turn section sizes → Vercel logs + m8_router_misses lane `ctx:packet`; sizes/labels only, no content | ✅ LIVE-VERIFIED | 21/21 | a94bfce | 2026-07-02 | Prod row confirmed (`L:fleet TOT:38201 …`). Kill switch `M8_CTX_TELEMETRY=off`; insert awaited ≤1.5s; feeds the context diet |
| — | STRATEGY H2-2026 locked: goal + Track B bar (engine→census artifact→Mathlib, obstruction-driven) + STOP list + E1–E8 roadmap + model assignment | ✅ LIVE | — | f80b59d/9105b94 | 2026-07-02 | `STRATEGY_2026H2.md`; Career OS + ecommerce OUT of M8; NORTH_STAR + diagram updated |

---

## Shipped — Session 58 (2026-06-20)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| 104 | Auto Bolt fleet cron: daily server-side sync → Supabase fleet_data (no manual CSV) | 🟡 DONE | — | e061395 | 2026-06-20 | vercel.json: cron 21:00 UTC (midnight Riyadh). Needs SUPABASE_URL + SUPABASE_SERVICE_KEY in dashboard Vercel. No tests (no Bolt API in CI). |
| 102 | "re-extract knowledge" chat command + /api/ingest-extract-existing wiring | 🟡 DONE | 54/54 | 17197e7 | 2026-06-20 | Books never at Step 1 — command is plumbing for future repairs. Source 6 (team brief junk) NOT extracted intentionally. |

---

## Shipped — Session 57 (2026-06-20)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| 103 | Provider health: GET /api/health — 8 providers + Supabase + quota graceful degradation | ✅ LIVE-VERIFIED | — | c140547 | 2026-06-20 | /api/health confirmed 6 providers configured |
| 101 | Atomic ingest pipeline: /api/ingest-full (store+extract) + /api/ingest-extract-existing | 🟡 DONE | 45/45 | 68317db | 2026-06-20 | Audit: 0 books ever persisted; 5 sources are Collatz snippets; source_doc_id (NOT source_id) |
| 100 | Driver profile manager: "set Ahmad rental 1800 SAR" → writes DB; list/delete via chat | ✅ LIVE-VERIFIED | 78/78 | 2f822d3 | 2026-06-20 | Ahmad profile confirmed created 03:05 PM; placeholder row auto-cleared |
| 99 | Outcome-biased proposer: AVOID + VERIFIED blocks injected into M4 conjecture gen | 🟡 DONE | — | 36611a3 | 2026-06-20 | runConjectureGenWithFeedback() wired; m8_conjecture_outcomes still 0 rows |
| 97 | Uber CSV passive: platform-schemas + platform-merge cross-platform dedup via entity slug | 🟡 DONE | 18/18 | c9044df | 2026-06-20 | Uber field aliases unconfirmed vs real export — gate on preview rowCount |
| 96 | Driver nudge logging: every Arabic nudge → m8_nudge_log (tone + driver + date) | 🟡 DONE | — | ee733a5 | 2026-06-20 | m8_nudge_log still 0 rows — no live nudges fired yet |
| 95 | Fleet intelligence report: per-driver company P&L + recommended actions | 🟡 DONE | 51/51 | 76d896a | 2026-06-20 | Blocked on real driver cost data — B100 now live so this can be live-tested |
| 93 | Multi-platform CSV: Bolt CSV parse + platform-sync pipeline | 🟡 DONE | — | 1cfa039 | 2026-06-20 | platform-schemas.js + platform-ingest.js + platform-sync.js |
| 92 | Conjecture learning loop: verified Lean leaves → proposer feedback | 🟡 DONE | 4/4 | 63ca9db | 2026-06-20 | ⚠️ migration B92_conjecture_outcomes.sql NOT APPLIED — table dormant |
| 91 | P&L engine: canonical pnl-engine.js — rental + 50% Bolt bonus = company revenue | 🟡 DONE | 6/6 | da56dba | 2026-06-20 | ⚠️ orchestrator.js lines ~1030-1039 still inline old gross-minus-costs — needs rewiring |
| 90 | Entity slug: Arabic/Latin dedup — Mohammed == Muhammad == Arabic form | 🟡 DONE | 39/39 | 4ffb576 | 2026-06-20 | Migration B90_entity_slug.sql applied ✓ |

---

## Shipped — Session 55-56 (2026-06-19/20)

| Build | What | Status | Tests | Commit | Date | Notes |
|-------|------|--------|-------|--------|------|-------|
| 88 | Proactive follow-up chips: M8-CHIPS after knowledge/general turns | 🟡 DONE | — | 6d5403f | 2026-06-20 | Chips appear after reflector pass |
| 87 | Driver cost profiles: driver_cost_profiles table schema + getCostProfile/computeRealPnl | 🟡 DONE | — | 6d5403f | 2026-06-20 | Table exists; data entry now via B100 chat commands |
| 86 | Longitudinal intelligence: recurring topics + trending entities in context | 🟡 DONE | — | 6d5403f | 2026-06-20 | m8_longitudinal_topics RPC applied ✓ |
| 85e | Memory consolidation: soft-merge Jaccard≥0.6 dups + contradiction flags | 🟡 DONE | 43/43 | 1301b08 | 2026-06-20 | Migration B85e applied ✓ |
| 85d | Multi-hop reasoning chain: complex Qs decompose → step-by-step chain | 🟡 DONE | 53/53 | 28dedc0 | 2026-06-20 | Migration B85d applied ✓ |
| 85c | Self-reflection loop: Gemini second-pass scores relevance/overclaim/missed-source | 🟡 DONE | 72/72 | — | 2026-06-19 | General + knowledge lanes only; fleet/finance gated out |

---

## Pending actions (not builds — data or config)

| Item | Owner | Blocker |
|------|-------|---------|
| Add SUPABASE_URL + SUPABASE_SERVICE_KEY to **dashboard** Vercel env | Muhammad | Go to vercel.com → mhmbolt project → Settings → Environment Variables |
| Enter real driver profiles (rental/salary/fuel per driver) | Muhammad | B100 live — type in M8 chat when ready |
| Book ingestion: bn01.txt → M8 chat | Muhammad | Quota reset — try tomorrow morning |
| Live-test B95 fleet report with real driver data | Muhammad | After driver profiles entered |

---

## How to update this file

**When a build report lands:** add a row under the correct session header immediately.
**When live-verified:** change 🟡 to ✅ and add the verification detail in Notes.
**When a pending action is done:** delete the row from the Pending actions table.
